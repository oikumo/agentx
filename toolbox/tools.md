# toolbox/tools — per-tool docs index (GENERATED by `toolbox.py docs --gen`)

> rev=26 tools=6 — contract: `<cat>/<name>/run.sh` (set -euo pipefail, uv-only) + `main.py` (JSON) + `TOOL.md` (<=15).

## git.recent_log
- category: git — recent log
- run: `uv run scripts/toolbox/toolbox.py run git.recent_log`
- entry: `toolbox/git/recent_log/run.sh` tags: `git,recent_log,promoted` uses=8 successes=8

## git.status_summary
- category: git — git porcelain status + branch summary
- run: `uv run scripts/toolbox/toolbox.py run git.status_summary`
- entry: `toolbox/git/status_summary/run.sh` tags: `git,status,dirty,porcelain` uses=18 successes=18

## harness.budget_check
- category: harness — wrap harnessc check budgets green
- run: `uv run scripts/toolbox/toolbox.py run harness.budget_check`
- entry: `toolbox/harness/budget_check/run.sh` tags: `harness,budget,check,limits` uses=9 successes=9

## ledger.window_slice
- category: ledger — slice last N ledger.jsonl lines with optional kind filter
- run: `uv run scripts/toolbox/toolbox.py run ledger.window_slice`
- entry: `toolbox/ledger/window_slice/run.sh` tags: `ledger,slice,tail,history` uses=9 successes=9

## session.pause_note_append
- category: session — stage a pause note entry (dry-run default)
- run: `uv run scripts/toolbox/toolbox.py run session.pause_note_append`
- entry: `toolbox/session/pause_note_append/run.sh` tags: `session,pause,note,resume` uses=9 successes=9

## text.json_split
- category: text — split JSON-array or bullet/newline prose behaviors
- run: `uv run scripts/toolbox/toolbox.py run text.json_split`
- entry: `toolbox/text/json_split/run.sh` tags: `json,split,tdd,behaviors,prose` uses=18 successes=18
