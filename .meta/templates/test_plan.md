# Test Report: {{TITLE}}

> **Phase:** Testing — `omt_agent_guide.md §11` | **Feature:** {{SLUG}}
> Run everything with `uv run pytest ...` (AGENTS.md MANDATORY).

## Stage 1 — Unit / component
| Component | Normal path | Exception paths | Result |
|-----------|-------------|-----------------|--------|
| | | | [ ] |

## Stage 2 — Integration
<!-- Controller+View (mock Model), Controller+Model (mock View), DP+in-memory SQLite -->

## Stage 3 — System (use-case driven)
<!-- Follow each use case's steps as a test script. -->

## Evidence
<!-- Replaces ad-hoc *_PROOF.md files. Paste the passing `uv run pytest` summary here. -->

```
$ uv run pytest tests/... -q
```
