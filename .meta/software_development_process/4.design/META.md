# 5. Design

> Architecture, component diagrams, interfaces, and design class diagrams per OMT++ Design Phase (§5-10).
> The application current structure overview is exposed in the folder `structure/` files.
> The application current behavior overview is exposed in the folder `behavior/` files.

