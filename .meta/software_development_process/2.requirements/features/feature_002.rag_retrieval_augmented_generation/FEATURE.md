# FEATURE

- Agentx RAG support RAG feature that allows keep in a session multiple RAG databases that can be loaded and make queries to it.
- The RAG data bases can be populated via WebSearch APIs, PDF documents and MD files.
