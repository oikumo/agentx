[project]
name = "meta-harness-knowledge-base"
version = "1.0.0"
description = "Knowledge Base with ChromaDB RAG"
requires-python = ">=3.12"
dependencies = [
 "chromadb>=0.4.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
