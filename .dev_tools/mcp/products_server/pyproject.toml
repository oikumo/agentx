[project]
name = "mcp-server-products"
version = "0.1.0"
description = "MCP server for managing product inventory with SQLite database"
requires-python = ">=3.14"
dependencies = [
    "mcp[cli]==1.27.0",
    "fastmcp==3.2.3",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
