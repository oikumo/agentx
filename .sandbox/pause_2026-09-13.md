# Pause — 2026-09-13 (feature_090 T2-7, meta_harness_8)

## Active task

- **feature_090.scaffolds_and_lsp_allowlist** (mh8 T2-7: scaffolds + LSP allowlist,
  minor_feature) — phase records in ledger: Analysis → Programming (both declared,
  Programming active). Canary skip for tests/ recorded. NOT yet complete — do not
  run `omt_complete` on resume until the remaining steps below are done.

## Completed this session

1. Scaffolded + linked feature → `meta_harness_8` (`new_feature.py ... --project`).
2. Analysis artifact written:
   `.meta/software_development_process/3.analysis/features/feature_090.scaffolds_and_lsp_allowlist/analysis_001_scaffolds_and_lsp_allowlist.md`
   — contains the FULL evidence base for both halves (recorded opencode edit-output
   shapes, live pyright 1.1.408 noise inventory: 9 errors = main_controller.py
   reportArgumentType×3 + reportAttributeAccessIssue×1, tui/provider.py
   reportMissingImports×5; test idioms; receipt/canary discipline).
3. **P3-10 IMPLEMENTED** in `scripts/omt/new_feature.py`: `testing` /
   `implementation` subcommands (`--feature <slug>`, `--date`, `--dry-run`);
   argv pre-scan before legacy positional path; unknown-slug + refuse-overwrite
   errors (rc=2); PROCESS_ROOT global added. Verified: both dry-run smokes OK,
   e2e receipt FRESH (run after last edit — file has NO unverified changes),
   `test_project_lifecycle.py` 23/23. TA: xref thought added @ new_feature.py:158.
4. Canary for tests/ edits obtained (D6 ordering held: phase → skip → tests).

## Immediate next steps (resume here)

1. Create `.opencode/lib/enforcer/lsp_filter.ts` — pure filter + thin entry, per
   analysis §Approach: gate on `metadata.diagnostics` severity-1 entries; allowlist
   `.meta/lsp_allowlist.json` (`{ "<repo-rel path>": ["<code>", ...] }`); drop
   allowlisted (file, code) pairs from metadata; RE-RENDER the trailing
   "LSP errors detected …" section of `output.output` from survivors (this-file vs
   other-files headers; `ERROR [L+1:C+1] message` lines); fail-open on any
   surprise. Idioms: see `mvc_after.ts` (module header style) + `relOf` in
   `omt_shared.ts` for abs→rel.
2. Create `.meta/lsp_allowlist.json` seeded with the 9 live entries (paths +
   codes listed in the analysis doc §Problem 2).
3. Register in `.opencode/plugins/omt_enforcer.ts` after-hook (one edit: call +
   header-comment module list) — receipt round-robin: ONE edit per file per round,
   `uv run pytest tests/scripts/omt/test_omt_harness_e2e.py -q` between rounds.
4. Add `.opencode/lib/enforcer/lsp_filter.ts` to `HARNESS_FILES` in
   `tests/scripts/omt/test_omt_harness_e2e.py` (receipt-EXEMPT file; canary
   already recorded this session — a NEW session must re-record
   `omt_skip{scope:"tests", purpose:"canary"}` before editing tests/).
5. Goldens: new `tests/scripts/omt/test_scaffolds_lsp_allowlist.py` —
   (a) new_feature subcommand tests (monkeypatch FEATURES_DIR/PROCESS_ROOT, `_mod`
   idiom from test_project_lifecycle.py): correct paths, refuse-overwrite rc=2,
   unknown-slug rc=2, legacy path byte-identical; (b) bun probes on lsp_filter
   pure functions using the RECORDED output shapes (analysis doc has exact
   samples): allowlisted 9 suppressed + planted-new-error (different code)
   surfaces + fail-open; (c) source pins (registration present).
6. `uv run scripts/omt/harnessc.py check && build` (watch nav_index budget
   64956/65536 — @doc record for the allowlist is OPTIONAL; skip if over).
7. e2e receipt + full suite `uv run pytest -q` (expect ~2173+new, 1 known
   environmental live-opencode flake).
8. Dogfood the new scaffolder for feature_090's own artifacts:
   `uv run scripts/omt/new_feature.py implementation --feature feature_090.scaffolds_and_lsp_allowlist`
   then `testing --feature ...`; fill them; update FEATURE.md traceability rows.
9. Update mh8 PROJECT.md §Status (+ T2-7 row) + CURRENT_STATE.md entry;
   `omt_complete{feature:"feature_090.scaffolds_and_lsp_allowlist", advance_to:"Done"}`.

## Blockers

- None. (Pre-existing: `sandbox/META_HARNESS_ROADMAP.md` modified in working tree
  before this session — NOT feature_090's, leave untouched.)

## Read-first artifact paths (resume entry)

1. This file.
2. `.meta/software_development_process/3.analysis/features/feature_090.scaffolds_and_lsp_allowlist/analysis_001_scaffolds_and_lsp_allowlist.md` (all evidence/contracts).
3. `.projects/meta/meta_harness_8/PROJECT.md` §T2-7 row + §Status (T2-7 not yet marked).
4. TA: thought at `scripts/omt/new_feature.py:158` (in-source resume pointer).

# Result

«paused at mid-Programming (P3-10 shipped+verified, P3-11 not started), resume by reading .sandbox/pause_2026-09-13.md → analysis_001 → 9-step list; WORK.md [~] row added under Paused (resumable); session safe to close»

## WORK.md

- [x] Updated: `[~]` entry added under **Paused (resumable)** pointing here.
- Session is safe to close. (Receipt state: e2e receipt FRESH as of this pause —
  next new_feature.py/lsp_filter.ts edits start from a clean receipt.)
