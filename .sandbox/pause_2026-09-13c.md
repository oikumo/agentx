# pause_2026-09-13c — meta_harness_8 T3-4 task-cost benchmark, mid-Programming

> Paused 2026-09-13 during Programming phase, BEFORE any bench code was written.
> Resume by reading this file, then `analysis_001_benchmark_design.md` IN FULL.

## Active task

- **T3-4 task-cost benchmark (first measurement)** — `feature_093.task_cost_benchmark`,
  minor_feature, project `meta_harness_8` (Improvement002 A via mh7 intake; mh8 D7 maps A→T3-4).
- Ledger state: `omt_phase` Analysis + Programming declared this session (active phase record,
  Programming, expires 8h — re-declare on resume).
- WORK.md: `[~]` row added under Paused (resumable).

## Completed so far (this session)

1. Source + acceptance traced: mh7 PROJECT.md §Improvement002 intake item A (line 142);
   mh8 PROJECT.md T3-4 row. Acceptance = repeatable script + first numbers, no policy change;
   target (not promise) −50% harness calls / −20% tokens small-task, all seeded violations caught.
2. D5 overlap check CLEAR: no `*benchmark*` files anywhere; mh7 baseline was ledger/suite
   snapshot only — A adds pinned task trials + token/time + seeded violations.
3. Infrastructure recon COMPLETE (the expensive part — all patterns verified against real code):
   - bun-probe pattern: `tests/scripts/omt/test_resume_digest.py` `_probe_template`
     (plugin `default({directory, worktree})` → `{tool}` → `.execute(args, {sessionID})`;
     root via `initOmtShared(sandboxRoot)`; JSON.stringify results — GOTCHA_PROBE_SERIALIZATION).
   - `gate_driver.ts`: `runBeforeGatesDry(ctx, irOverride?)` → per-gate
     `{gate_id, blocked, msg, skip_ok, fired, stop}` — the trial's decision transcript AND the
     removal-experiment mechanism (filter `ir.gates` by id → irOverride; zero production changes).
   - `omt_enforcer.ts` default export (lines 57–137) = the hook-bus contract the probe must
     replicate: before = `navTrack`+`kbTrack`+`runBeforeGates`; after = `sessionBootstrap`
     (once/session) + `trackRead` + `injectThoughtsOnRead` + `lspAfterEdit` + `runAfterGates`.
   - Shared-env model: ONE `EnforcerEnv`+`createSessionState()` for gates AND
     `createPhaseTools(env)`/`createTddTools(env)` (both exported); omt_status/think/nav/kb/net
     stay plugin-default (their effects reach gates via ledger records + trackers, NOT in-memory).
   - Worktree facts: only `harness.ir.json` + `nav.index.jsonl` committed under `.meta/.omt/`;
     ledger + receipts + net state gitignored → fresh worktree = valid harness, clean ledger,
     fail-closed g.receipt (the harness-repair ceremony to measure); concurrent task setup must
     copy net state + inject a T-bench binding (`test_net_task_claim_generation.py::_pool_bundle`
     pattern; hermetic env vars OMT_NET_DIR/OMT_LEDGER_PATH/OMT_COORDINATION_ROOT).
4. **`analysis_001_benchmark_design.md` WRITTEN** (the complete design — 3-layer architecture,
   6-task table with exact seeded faults, metrics defs, removal model, goldens list, modeling
   assumptions) + 3 TA: thoughts (probe env model, irOverride removal, worktree facts).
5. Task targets locked with exact anchors:
   - bugfix: `src/agentx/model/chat/chat_history.py` `ORDER BY timestamp ASC`→`DESC`
     (breaks `test_get_messages_returns_in_order`; `tests/unit/model/test_chat_history.py`
     26 passed / 0.13s at HEAD).
   - cross-layer: same model fault + `src/agentx/ui/screens/chat/chat_controller.py`
     `for msg in messages:\n            if msg.role == "user":` → `for msg in reversed(messages):...`
     (multi-line anchor — the loop appears twice; this disambiguates).
   - major (TDD): new helper in `src/agentx/utils/utils.py` (NO TA thoughts there — think-gate
     clean) + new test file; anchor for append = file tail `    return True` after
     `dangerous_delete_directory` print block.
   - harness repair: `scripts/omt/harnessc.py` `BUDGET_DIET_PROXIMITY = 64`→`65` (breaks
     test_budget_diet 64/65 boundary golden; e2e source pins stay green). Stage CLI:
     `uv run scripts/omt/harnessc.py stage --feature <slug> <files...>`; boundary e2e =
     `tests/scripts/omt/test_omt_harness_e2e.py`; clear with `stage --clear`.
     NOTE: stage --feature must name a feature that EXISTS at pinned HEAD (feature_092.resume_digest),
     not feature_093 (uncommitted).
   - resume: ledger seed shape = `test_resume_digest.py::_seeded_records`
     (phase/tdd/think_consult/project_link records, ts ~1h ago); digest path exercises
     feature_092; bounded partial reads with offset/limit; counterfactual = full re-read bytes.
   - concurrent: A claims T-bench → wins; B double-claim refused; edit
     `scripts/omt/net/state.py` w/o work_start → g.net block → `omt_net fire work_start` →
     checkpoint; B stale-generation claim refused.
6. `feature_093.task_cost_benchmark` scaffolded + linked; omt_phase Analysis + Programming
   declared; preflight already read: new golden file needs tests canary (g.tests), src/ edits in
   bench development itself are NOT gated (scripts/omt/bench is outside src/ + HARNESS_FILES →
   NO e2e receipt round needed for this feature).

## Immediate next step (resume here)

Write the bench package (all design decisions in analysis_001; nothing to re-derive):
1. `scripts/omt/bench/__init__.py` + `model.py` (Step/TaskDef dataclasses, kinds
   omt/edit/read/search/bash/verify/assert, roles work/intervention/recovery/verify/violation,
   optional `gate:` annotation on ceremony steps, validate_task) + `tasks.py` (6 real + 2 fixture
   tasks per the analysis table) + `sandbox.py` (real: `git worktree add --detach`, fault
   replacement application, net-state copy + T-bench injection, ledger seeding, feature
   pre-scaffold for major task; fixture: copy `.opencode/` + compiled `.meta/.omt/` projections +
   synthetic src/tests incl. plain-python test runner script; `read_deny_rules` =
   opencode.jsonc parse → bash/read deny patterns) + `probe.ts` (trial runner per the hook-bus
   contract) + `metrics.py` + `report.py` + `cli.py` + `scripts/omt/task_cost_benchmark.py` shim.
2. Canary (`omt_skip{scope:"tests", purpose:"canary}` after re-declaring phase) then goldens
   `tests/scripts/omt/test_task_cost_benchmark.py` — 8 groups listed in analysis_001 §Goldens
   (incl. bun-gated fixture trial + removal variants).
3. First-numbers real run at pinned HEAD → `bench_first_numbers.{json,md}` under
   `6.testing/features/feature_093.task_cost_benchmark/`; unexpected FP blocks are FINDINGS
   (friction), not failures.
4. Close out: `harnessc check && build`, full suite, test_report.md + impl notes
   (new_feature.py testing/implementation scaffolders), `omt_complete`, CURRENT_STATE.md entry
   (meta_harness_8), WORK.md row flip.

## Blockers

None.

## Artifacts to read first (resume order)

1. This file.
2. `.meta/software_development_process/3.analysis/features/feature_093.task_cost_benchmark/analysis_001_benchmark_design.md` — IN FULL (the design SSOT; TA: thoughts at lines 111–113).
3. `.projects/meta/meta_harness_8/PROJECT.md` — §The 5 tracks T3-4 row + D6/D7 (discipline).
4. Probe pattern references (only if needed): `tests/scripts/omt/test_resume_digest.py`
   (_probe_template + _seeded_records), `test_gkb_read_recency.py` (env construction),
   `test_net_task_claim_generation.py` (_pool_bundle).

## Result

Paused at bench-package implementation start (analysis complete, zero bench code written).
Resume by reading `.sandbox/pause_2026-09-13c.md` → analysis_001 → write the package.

**WORK.md updated 2026-09-13** (`[~]` row added under Paused (resumable), user-confirmed) —
session is safe to close. On resume: re-declare `omt_phase{minor_feature, Programming}` (the
active phase record expires 8h), then continue at "Immediate next step" above.
