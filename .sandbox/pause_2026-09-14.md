# pause_2026-09-14 — meta_harness_8 T3-4 task-cost benchmark, IMPLEMENTATION phase (bench package + goldens + first numbers)

> Supersedes `.sandbox/pause_2026-09-13f.md` (which stays canonical for its
> session-4 recon: analysis_001 TA:123–126). Paused 2026-09-14 (5th pause of
> T3-4, 1st WITH code): the bench package is written, 17/17 goldens green,
> first-numbers real run recorded at pinned rev `81b3aa1` — 4/6 tasks fully
> green, 2 with diagnosed-but-unfixed findings. Resume by reading this file,
> then §Immediate-next-step. Do NOT re-derive the design (analysis_001 +
> pause_f + pause_e + pause_d stay canonical).

## Active task

- **T3-4 task-cost benchmark (first measurement)** — `feature_093.task_cost_benchmark`,
  minor_feature, project `meta_harness_8` (Improvement002 A via mh7 intake; mh8 D7 A→T3-4).
- Ledger: phase record minor_feature/Programming ACTIVE (re-declared 2026-09-14;
  scope "Implement bench package (model/tasks/sandbox/probe.ts/metrics/report/cli)
  + shim + goldens; then first-numbers real run + artifacts.") + canary
  `omt_skip{scope:"tests", purpose:"canary"}` for the golden file — re-declare both on resume.
- Acceptance (unchanged): repeatable script + first numbers, no policy change; target
  (not promise) −50% harness calls / −20% tokens small-task, all seeded violations caught.

## Completed this session (delta over pause_2026-09-13f.md)

1. **Bench package written** — `scripts/omt/bench/` (8 files) + `scripts/omt/task_cost_benchmark.py`
   shim, all in receipt-round discipline (fresh boundary e2e before every second-edit round):
   `model.py` (Step/TaskDef + validate_task), `tasks.py` (6 real + 2 fixture defs),
   `sandbox.py` (worktree + fault seeding + net 3-binding injection + resume/major seeds +
   uv.lock copy + pre-warm), `probe.ts` (ONE shared EnforcerEnv, hook-bus replication,
   deny pre-spawn, JSON.stringify transcript), `metrics.py` (unified refusal TP/FP/missed,
   success = verify/assert only), `report.py` (markdown + `bench_first_numbers.v1` JSON),
   `cli.py` (list/run/removal/run-all + string-aware JSONC deny parser).
2. **Two task-design bugs found by real-run friction and fixed** (manual bash round + e2e):
   (a) `bun test tests/test_app.js` never matches (needs `./` prefix or `.test` in name) —
   fixture verifies now use `bun test ./tests/test_app.js`;
   (b) `ORDER BY timestamp` ties are stable (CURRENT_TIMESTAMP is 1s resolution) so the
   ASC→DESC fault never broke the test — seeded fault is now `ORDER BY id DESC`
   (reliably reverses), fix restores `ORDER BY timestamp ASC`; regression append sleeps 1.1s.
3. **Goldens green** — `tests/scripts/omt/test_task_cost_benchmark.py`, 17/17 (8 groups per
   analysis_001 §Goldens: validation, metrics math, removal accounting, report render, deny
   parsing incl. $schema-URL `//` case, bun-gated fixture trials, bun-gated removal variants,
   CLI list). NOTE: file has a wrong-path bug history (`parents[4]` → fixed to `parents[3]`);
   current content is correct — do NOT "fix" the parents index again.
4. **First-numbers real run recorded** at pinned rev `81b3aa1` →
   `.meta/software_development_process/6.testing/features/feature_093.task_cost_benchmark/bench_first_numbers.{json,md}`
   (schema `bench_first_numbers.v1`): bugfix ✅ / cross_layer ✅ / major ✅ (TDD RED+GREEN,
   6 harness calls) / resume ✅ (orientation 288B) / harness_repair ❌ (regressions=1) /
   concurrent_conflict ❌ (missed=2). Per-task table in the JSON (`per_task`).
5. **Housekeeping**: stale `.sandbox/bench/*` worktrees removed + `git worktree prune`
   (a kept debug worktree once blocked `setup_real` with "already registered").

## Immediate next step (resume here)

1. **Diagnose the 2 non-green trials WITH resultTexts** (the probe transcript drops
   `resultText` to save bytes — temporarily log it, e.g. `bun` one-off against a kept
   sandbox `--keep`, or add a `--verbose` probe flag):
   - `concurrent_conflict` (missed=2: `b_double_claim` + `b_stale_claim` slipped in-trial,
     yet manual fresh-sandbox claims behave: A→`{"ok":true}` OBJECT, B-double→`task_not_pending`,
     B-stale(exp 0)→`revision_mismatch`). Prime suspect: probe `execOmt` always
     `JSON.stringify`s the omt_net result — success OBJECT becomes `'{"ok":true,...}'`
     (NO space) while the golden assert looks for `'"ok": true'` (WITH space) → `assert_claim`
     fails; and a shared shape mismatch may hide the two refusals the same way. Also
     `expected_revision: 0` is a REAL check (mismatch vs current rev), not "no check" —
     resolve the TA:119 placeholder properly: patch `b_stale_claim.expected_revision` to the
     setup revision at spec build (`setup["setup_revision"]`, currently `"57"`) in
     `cli.run_trial` after `setup_real`, so the claim is genuinely stale post-A-claim.
   - `harness_repair` (regressions=1, success=false, missed=0 — ceremony caught everything;
     one `expect=pass` verify got rc≠0 in the worktree: identify which of diagnose/e2e/golden
     and record as FINDING or fix the anchor).
2. Apply the fixes (receipt round-robin: ONE edit/file/round + fresh boundary e2e between
   rounds; tests/ file needs the canary re-recorded after phase re-declare), re-run
   `uv run pytest tests/scripts/omt/test_task_cost_benchmark.py -q` (must stay 17/17),
   re-run `uv run scripts/omt/task_cost_benchmark.py run-all --mode real` to refresh the
   first-numbers artifacts.
3. Close out: `uv run scripts/omt/harnessc.py check && build`, full suite
   (`uv run pytest -q`), test_report.md + impl notes via `new_feature.py testing/implementation
   --feature feature_093.task_cost_benchmark`, `omt_complete`, CURRENT_STATE.md (meta_harness_8)
   entry, WORK.md row flip to [x], archive pause c+d+e+f+g (this file).
   Open repair flag (unchanged): `meta_harness_development_self_evaluation.md` unindexed.

## Blockers

None. (Two trial findings diagnosed-to-suspect, not blocked; all fixes are in new/existing
bench files, no production policy change.)

## Artifacts to read first (resume order)

1. This file.
2. `.meta/software_development_process/3.analysis/features/feature_093.task_cost_benchmark/analysis_001_benchmark_design.md`
   — IN FULL incl. all 16 TA: thoughts (111–126).
3. `.sandbox/pause_2026-09-13f.md` (session-4 recon delta) + `.sandbox/pause_2026-09-13e.md`
   §Completed 1–10 + `.sandbox/pause_2026-09-13d.md` §Completed 1–9 (still canonical).
4. New code: `scripts/omt/bench/` (8 files) + `scripts/omt/task_cost_benchmark.py` +
   `tests/scripts/omt/test_task_cost_benchmark.py` + `bench_first_numbers.{json,md}` under
   `.meta/software_development_process/6.testing/features/feature_093.task_cost_benchmark/`.
5. Reference patterns (only if needed): `tests/scripts/omt/test_resume_digest.py`
   (`_probe_template`, `_seeded_records`, GOTCHA_PROBE_SERIALIZATION),
   `.opencode/lib/enforcer/gate_driver.ts` (`runBeforeGatesDry`),
   `.opencode/plugins/omt_enforcer.ts` (hook bus), `scripts/omt/net/state.py`.

## Idempotency

This pause is the current resumption artifact for T3-4. Re-running the pause workflow on
an unchanged session state must NOT create pause_2026-09-14b — update THIS file in place
(and only bump the letter/date after substantive new work).

## Result

**WORK.md updated 2026-09-14** (user-confirmed; `[~]` row now points here, superseding
pause_2026-09-13f.md) — session is safe to close. On resume: re-declare
`omt_phase{task_type:"minor_feature", phase:"Programming",
feature:"feature_093.task_cost_benchmark", scope:"Implement bench package
(model/tasks/sandbox/probe.ts/metrics/report/cli) + shim + goldens; then first-numbers real
run + artifacts."}` + canary `omt_skip{scope:"tests", purpose:"canary"}`, then continue at
"Immediate next step" above.
