# pause_2026-09-13d — meta_harness_8 T3-4 task-cost benchmark, bench-package implementation ready

> Supersedes `.sandbox/pause_2026-09-13c.md`. Paused 2026-09-13 (2nd pause of T3-4) after
> COMPLETE implementation-readiness recon — every reference pattern read in full, every
> design unknown resolved, zero bench code written. Resume by reading this file, then
> analysis_001 (now 6 TA: thoughts) — then WRITE THE PACKAGE. Nothing left to derive.

## Active task

- **T3-4 task-cost benchmark (first measurement)** — `feature_093.task_cost_benchmark`,
  minor_feature, project `meta_harness_8` (Improvement002 A via mh7 intake; mh8 D7 A→T3-4).
- Ledger: active phase record minor_feature/Programming (declared 2026-09-13, 8h expiry —
  re-declare on resume via `omt_phase{task_type:minor_feature, phase:Programming,
  feature:feature_093.task_cost_benchmark, scope:...}`).
- Acceptance: repeatable script + first numbers, no policy change; target (not promise)
  −50% harness calls / −20% tokens small-task, all seeded violations caught.

## Completed this session (delta over pause_2026-09-13c.md)

All reference patterns read IN FULL and locked (this was the remaining risk):

1. **Hook-bus contract** (omt_enforcer.ts 57–137 + all 6 lib modules in full):
   before = `navTrack`+`kbTrack`+`runBeforeGates(env,session,input,output,raw)`;
   after = `sessionBootstrap`+`trackRead`+`injectThoughtsOnRead`+`lspAfterEdit`+
   `runAfterGates(env,session,input,output,raw)`. `GateCtx = {env, session, tool, input,
   output, rel, abs, memo}`; before-hook args on OUTPUT, after-hook args on INPUT.
2. **Trial-runner mechanism**: `runBeforeGatesDry(ctx, irOverride?)` → per-gate
   `{gate_id, blocked, msg, skip_ok, fired, stop}` (removal experiments = filter
   `ir.gates` by id → irOverride; zero production changes). Blocked step ⇒ probe must NOT
   apply the effect and must NOT run the after-chain (real opencode semantics).
   IR gate orders confirmed live: g.nav 0, g.protect 10, g.receipt 20, g.tests 30,
   g.net 35, g.phase 40, g.think 50, g.kb 55; after: g.mvc 60, g.tdd_after 70.
3. **Tool surfaces**: `createPhaseTools(env)` → {omt_phase, omt_skip, omt_complete};
   `createTddTools(env)` → {omt_tdd} (both need env.$ shell); plugin defaults
   (omt_status/omt_think/omt_nav/omt_kb_nav/omt_net) all take
   `default({directory, worktree})` → `.tool.omt_*` (resume-digest probe pattern;
   GOTCHA_PROBE_SERIALIZATION: JSON.stringify object results).
4. **⭐ TWO-HATS DEADLOCK + SKIP SHADOWING RESOLVED** (ledger archaeology, feature_043/
   050): testlist hat blocks ALL edits incl. tests/ (canary NOT consulted under tdd_mode),
   but omt_tdd red needs an existing failing test → real bootstrap = omt_skip{scope:tests,
   "RED bootstrap — planning-hat deadlock"}. SIDE EFFECT: getActiveUnlock is latest-phase-
   OR-skip-wins → the skip shadows the tdd phase record → `unlock.record.tdd_mode`
   undefined → two-hats NOT enforced on src/ for the rest of the session (impl between red
   and green was written un-two-hatted — reality). Major-task violation placement fixed
   accordingly (see TA: thought @ analysis_001:112): "edit src during RED hat" violation
   goes BEFORE the canary skip (testlist hat blocks, attribution g.phase); post-red
   pre-kb src edit is caught by g.kb; impl-after-kb passes with two-hats shadow-off =
   FINDING (friction/coverage signal), not failure.
5. **g.net engagement**: isConcurrentMarking = work_active>1 OR 2+ f\d+_active holders;
   live net: work_active=0, work_pending=0, NO task_bindings, no f\d+_active, rev 57 →
   concurrent setup injects 3 bindings + tokens (T-bench for A; one pre-claimed in setup
   by a 2nd owner; one spare pending so fire work_start still has a work_pending token).
   Probe env.$ MUST be a real shell (Bun.$ from "bun" — .cwd/.quiet/.nothrow tagged
   template API) else g.net silently skips its live shell-out.
6. **Cost shapes pinned**: omt_phase{Programming,tdd} runs FULL suite (120s cap,
   -m "not opencode_live") for baseline; omt_complete validate-exit runs FEATURE-scoped
   tests only (30s/node) + net_check invariant (fail-open) + WORK.md sync. Worktree has
   NO .opencode/node_modules (gitignored) → baseline full-suite has pre-existing bun-import
   failures (real; lands in baseline_failures). Probe imports lib from LIVE .opencode +
   initOmtShared(sandbox) — equivalent at pinned HEAD, no node_modules copy needed.
7. **Deny model**: driver parses opencode.jsonc permission (bash deny: git commit/push,
   python/python3/pip/pip3/pytest; read deny: *.env, *.env.*) → probe enforces pre-spawn;
  counted in blocks_tp, NOT removal-experimented. Verify/bash commands must match allow
   patterns (uv */bun *): fixture runner = bun script; real tasks = `uv run pytest …`.
8. Task targets re-verified at HEAD: chat_history.py:249 `ORDER BY timestamp ASC`;
   chat_controller.py loop at 169 AND 180 (multi-line anchor disambiguates);
   harnessc.py:1285 `BUDGET_DIET_PROXIMITY = 64`; utils.py tail `    return True`;
   tests/unit/model/test_chat_history.py present. harnessc stage CLI:
   `stage --feature feature_092.resume_digest <files...>` / `--status` / `--clear`
   (feature must exist at pinned HEAD — NOT feature_093).
9. Three new TA: thoughts added to analysis_001 (lines 112/114/116 — deadlock+shadowing,
   g.net engagement+3-binding, probe-import+Bun.$+cost shapes) — 6 total.

## Immediate next step (resume here)

Write the bench package — every decision is now made (analysis_001 + the 6 TA thoughts +
this file; nothing to re-derive):
1. `scripts/omt/bench/__init__.py` + `model.py` (Step/TaskDef dataclasses; kinds
   omt/edit/read/search/bash/verify/assert; roles work/intervention/recovery/verify/
   violation; optional `gate:` annotation; validate_task) + `tasks.py` (6 real + 2 fixture
   tasks; major-task step list per TA:112 placement fix) + `sandbox.py` (real:
   `git worktree add --detach <dir> HEAD` + fault seeding + net copy/3-binding injection +
   ledger seeding (resume task, `_seeded_records` shape) + design-doc pre-scaffold (major)
   + uv pre-warm; fixture: harness.ir.json+nav.index.jsonl copies + synthetic src/tests +
   bun test runner) + `probe.ts` (hook-bus replication per §Completed 1–3; Bun.$ env;
   blocked-step = no effect + no after-chain) + `metrics.py` (pure transcript accounting)
   + `report.py` (markdown+JSON, stable schema, assumptions section) + `cli.py`
   (list/run/removal) + `scripts/omt/task_cost_benchmark.py` shim.
2. Canary (`omt_skip{scope:"tests", purpose:"canary"}` after re-declaring phase) then
   goldens `tests/scripts/omt/test_task_cost_benchmark.py` — 8 groups per analysis_001
   §Goldens (validation, metrics math, removal accounting, report render, deny parsing,
   bun-gated fixture trial, bun-gated removal variants, CLI list).
3. First-numbers real run at pinned HEAD → `bench_first_numbers.{json,md}` under
   `.meta/software_development_process/6.testing/features/feature_093.task_cost_benchmark/`;
   unexpected FP blocks = FINDINGS (friction), not failures.
4. Close out: `uv run scripts/omt/harnessc.py check && build`, full suite, test_report.md +
   impl notes (new_feature.py scaffolders), omt_complete, CURRENT_STATE.md (meta_harness_8),
   WORK.md row flip, archive pause c+d.

## Blockers

None. (Pre-pause open question — the two-hats RED-bootstrap deadlock — is RESOLVED,
see §Completed 4 + TA:112.)

## Artifacts to read first (resume order)

1. This file.
2. `.meta/software_development_process/3.analysis/features/feature_093.task_cost_benchmark/analysis_001_benchmark_design.md`
   — IN FULL incl. the 6 TA: thoughts (111–116; 112 = violation-placement fix, 114 =
   g.net/3-binding + Bun.$, 116 = probe-import + cost shapes).
3. Reference patterns (only if re-verification needed): `tests/scripts/omt/test_resume_digest.py`
   (_probe_template/_seeded_records), `.opencode/plugins/omt_enforcer.ts` (hook bus),
   `.opencode/lib/enforcer/{gate_driver,session_state,phase_gate,tdd_hats,nav_gate,receipt_guard,policy_decision}.ts`,
   `tests/scripts/omt/test_net_task_claim_generation.py` (_pool_bundle).

## Result

Paused at bench-package implementation start, round 2 — recon now COMPLETE (was the
expensive remaining risk; includes the two-hats skip-shadowing discovery, a benchmark
finding in its own right). Resume by reading this file → analysis_001 (+TA thoughts) →
write `scripts/omt/bench/` package.

**WORK.md updated 2026-09-13** (user-confirmed; `[~]` row now points here, superseding
pause_2026-09-13c.md) — session is safe to close. On resume: re-declare
`omt_phase{minor_feature, Programming, feature_093.task_cost_benchmark}` (the active
phase record expires 8h), then continue at "Immediate next step" above.
