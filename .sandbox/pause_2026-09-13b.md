# Pause — feature_092.resume_digest (mh8 T3-3), mid-Programming

> Paused 2026-09-13 per `.workflows/meta_harness/pause_dev_for_resume_later.md`.
> Reversible + idempotent: this file is the single resume entry point.
>
> **RESULT (post-confirmation):** user confirmed the resumption document
> accurate 2026-09-13; WORK.md `## Paused (resumable)` carries the `[~]` row
> pointing here; ledger holds phase Programming + tests canary for
> feature_092.resume_digest; T4-2 stage ACTIVE on disk. **Session safe to
> close.** Resume: read this file → design note → `omt_status{op:"resume"}.`

## Active task

- **feature_092.resume_digest** — mh8 T3-3 resume digest (minor_feature),
  project `meta_harness_8`. `omt_phase` Programming declared this session
  (ledger), tests canary recorded (scope=tests), think-gate consult done
  (0 thoughts for .omt).

## Completed so far (all on disk, uncommitted)

1. **D5 overlap check** — omt_q is feature-scoped (gates/TDD/escape/delegate
   folds); omt_status digest is process-scoped. Complementary, no overlap.
2. **Design note** —
   `.meta/software_development_process/4.design/features/feature_092.resume_digest/design_001_resume_digest.md`
   (mechanism, budget math, acceptance, edit surface — READ THIS FIRST).
3. **Scaffold** — `new_feature.py "resume digest" --type minor_feature
   --project meta_harness_8` → feature_092.resume_digest created + linked.
4. **Implementation COMPLETE** in `.opencode/plugins/omt_status.ts`:
   - helpers: `byteLen`/`clipText`/`firstHeadingLine`/`quickStartNext`/
     `fileSizeB`/`trailLabel`/`resumeTrail`/`featureArtifactLine`/
     `buildResumeDigest` (byte-precise cap, drop-optional-then-hard-cut)
   - `op:"resume"` branch in execute (before unknown-op check; after preflight)
   - describes: op `| resume` (+9B), include_ledger diet (−8B)
   - fallback description synced to new .omt text
   - **manually verified working**: debug probe returned 432B digest with
     banner/project/next/trail/docs all correct.
5. **059 re-pins** — `tests/features/feature_059.harness_tiered_template/test_budget_pins.py`:
   TOOL_ARGS_CEIL 2454→2455, TOOL_SCHEMAS_CEIL 1812→1840 (dated comments).
6. **.omt** — `@tool omt_status` description updated (edited LAST, re-staged).
7. **harnessc check OK** (265 records, 0 errors) **+ build OK** (5 projections);
   budgets green: tool_args 2455/2464, tool_schemas 1840/1856, all others
   unchanged.
8. **Goldens written** — `tests/scripts/omt/test_resume_digest.py` (7 tests)
   + TA GOTCHA_PROBE_SERIALIZATION added at the `_probe` def.

## Immediate next step (the ONE blocker)

The 7 goldens fail with JSONDecodeError — **probe serialization only, logic is
good**: the probe template's `console.log(result)` prints bun inspect format
for omt_status's plain-object return (omt_q returns a JSON *string*, which is
why the delegate-fold probes pass with the same pattern).

**Fix (one line):** in `tests/scripts/omt/test_resume_digest.py`
`_probe_template`, change `console.log(result)` →
`console.log(JSON.stringify(result))` (precedent: test_task_prep_slice.py:29),
then re-run: `uv run pytest tests/scripts/omt/test_resume_digest.py -q`
(expected 7/7 green — manual probe already showed correct 432B output).

## Then, in order (the rest of the T4-2 staged batch)

1. Boundary e2e (validates the staged batch, writes the receipt):
   `uv run pytest tests/scripts/omt/test_omt_harness_e2e.py -q`
2. `uv run scripts/omt/harnessc.py stage --clear` (stage is ACTIVE on disk:
   `.meta/.omt/omt_harness_stage.json`, 4 files — do NOT re-stage, just e2e
   then clear)
3. `uv run scripts/omt/harnessc.py check && uv run scripts/omt/harnessc.py build`
   (re-verify)
4. Full suite `uv run pytest -q` (expect green; baseline was 2207 + 7 new =
   2214; known pre-existing: 059-era environmental live-opencode flake)
5. `omt_complete{feature:"feature_092.resume_digest"}` (Testing phase first if
   the harness asks)
6. Update `.projects/meta/meta_harness_8/PROJECT.md` §Status (T3-3 row) +
   CURRENT_STATE.md newest entry; remaining backlog after this: T3-4 / T3-6,
   then T5-8 loop.

## Blockers

- None external. Only the one-line probe fix above.

## Artifacts a future session reads FIRST (in order)

1. This file
2. `.meta/software_development_process/4.design/features/feature_092.resume_digest/design_001_resume_digest.md`
3. `git status --short` + `git diff` (all edits are the 4 staged files +
   scaffolded feature dirs + this pause file)
4. `omt_status{op:"resume"}` — dogfood: the new op itself orients the resume
   (that's the feature working for you)

## Stage state (T4-2 receipt batch — ACTIVE)

`harnessc stage --feature feature_092.resume_digest` holds 4 files:
`.opencode/plugins/omt_status.ts`, `.meta/META_HARNESS.omt`,
`tests/scripts/omt/test_resume_digest.py`,
`tests/features/feature_059.harness_tiered_template/test_budget_pins.py`.
Re-staged AFTER the .omt edit (089 policy_ver discipline) — e2e at the
boundary, then `stage --clear`.
