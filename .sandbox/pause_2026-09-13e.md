# pause_2026-09-13e — meta_harness_8 T3-4 task-cost benchmark, design FULLY LOCKED (recon round 3)

> Supersedes `.sandbox/pause_2026-09-13d.md` (which stays canonical for its §Completed 1–9
> hook-bus contracts). Paused 2026-09-13 (3rd pause of T3-4) after a full session-3 recon
> re-verification at HEAD: every reference pattern re-read in a fresh context, every remaining
> design decision locked, one pause_d claim CORRECTED, six new TA: thoughts persisted to
> analysis_001 (lines 117–122). Zero bench code written. Resume by reading this file, then
> analysis_001 (all 12 TA thoughts) — then WRITE THE PACKAGE. Nothing left to derive.

## Active task

- **T3-4 task-cost benchmark (first measurement)** — `feature_093.task_cost_benchmark`,
  minor_feature, project `meta_harness_8` (Improvement002 A via mh7 intake; mh8 D7 A→T3-4).
- Ledger: phase record minor_feature/Programming ACTIVE at pause (declared 2026-09-13 20:52,
  8h expiry) — re-declare on resume via `omt_phase{task_type:minor_feature, phase:Programming,
  feature:feature_093.task_cost_benchmark, scope:"Implement bench package
  (model/tasks/sandbox/probe.ts/metrics/report/cli) + shim + goldens; then first-numbers real
  run + artifacts."}`.
- Acceptance (unchanged): repeatable script + first numbers, no policy change; target (not
  promise) −50% harness calls / −20% tokens small-task, all seeded violations caught.

## Completed this session (delta over pause_2026-09-13d.md)

Fresh-context re-verification of every reference pattern at HEAD (all read in full this
session) + the following NEW discoveries/locks (each persisted as a TA: thought in
analysis_001 — see lines 117–122):

1. **⭐ CORRECTION to pause_d §"No policy change" guardrail** (TA:117): `scripts/omt/` IS an
   IR harness_paths prefix → `scripts/omt/bench/*` is g.receipt harness surface in the LIVE
   session too. Writing the package rides the one-edit-per-file-per-e2e-receipt round-robin
   (first write of a not-yet-existing file passes — omtHarnessE2eStatus returns ok when the
   file doesn't exist; any SECOND edit of a dirty file needs a fresh boundary e2e
   `uv run pytest tests/scripts/omt/test_omt_harness_e2e.py -q` or `harnessc stage`).
   pause_d's "bench is outside HARNESS_FILES / no e2e receipt needed" was WRONG
   (HARNESS_FILES = the e2e's own covered-file list, a different mechanism).
2. **g.net fail-closed ⇒ net-state copy for EVERY sandbox** (TA:118): the g.net impl denies
   (evaluatePolicy, no shell-out) when net_state.sidecar.json is unreadable → all 6 real
   sandboxes AND both fixtures must copy live net state (META_NET.petri.json +
   net_state.sidecar.json; live rev 57, work_active=0 → solo skip), else every
   src/tests/scripts edit is g.net-blocked as FP. Concurrent setup: 3 bindings (T-bench
   pending; one PRE-CLAIMED active by worker-2; one spare pending), live_marking
   work_pending=2 work_active=1, agent_attention=1 feature_ready=1 unchanged (work_start
   inputs {agent_attention:1, feature_ready:1, work_pending:1} confirmed live) → after A's
   claim work_active=2 (concurrent), fire work_start still enabled.
3. **Concurrent-task step ORDER fixed** (TA:119): net/gate.py fire receipt = ANY net_fire
   *_start ledger record within 8h, SESSION-AGNOSTIC → order MUST be A claim → B
   double-claim (engine task_not_pending) → B no-work_start edit (g.net blocks) → B
   stale-rev claim (engine stale_revision; expected_revision = placeholder resolved to the
   setup revision) → A fire work_start → A edit scripts/omt/net/state.py (allowed via
   receipt; committed-clean ⇒ g.receipt passes) → A checkpoint. B's edit AFTER A's fire
   would PASS (missed violation).
4. **Unified refusal metric + fixture_nophase read-first** (TA:120): refused := gate-block OR
   opencode.jsonc deny (bash/read, driver-side pre-spawn) OR engine envelope ok:false /
   ❌|⛔ string (omt_net claim refusals count TP). blocks_tp = role=violation ∧ refused;
   success = verify/assert expectations only (missed violations don't flip success — removal
   runs stay "successful but leaking"). fixture_nophase reads src/app.js FIRST so g.kb
   read-recency passes and the no-phase violation attributes to g.phase alone (golden #7).
5. **Major-task concrete shape locked** (TA:121): slug feature_990.bench_major (needs
   feature_\d+ for design-artifact auto-detect); setup pre-scaffolds
   4.design/features/feature_990.bench_major/design_001_bench.md + stub
   src/agentx/bench_major_demo.py (`def bench_answer() -> int: return 0`) so the RED test
   (tests/features/feature_990.bench_major/test_bench_major.py::test_bench_answer, asserts
   ==42) collects and fails exit-1 (nonexistent module = exit 2 = no red record).
   Violation-1 (src edit during testlist hat) BEFORE the canary skip, NO prior read →
   blocked_by [g.phase, g.kb] in dry transcript, real chain blocks at g.phase (40<55);
   violation-2 post-red blind edit → g.kb alone; skip-shadows-phase ⇒ impl edit +
   g.tdd_after un-two-hatted = FINDING (TA:112c).
6. **Resume-task concrete shape locked** (TA:122): ledger seeded (1h-old phase
   minor_feature/Programming feature_991.bench_resume + project_link bench_resume + trail,
   _seeded_records shape from test_resume_digest.py) + mini
   .projects/meta/bench_resume/{PROJECT.md,CURRENT_STATE.md}; steps = omt_status{op:resume} →
   bounded partial reads (tag=orientation) → tiny continuation (append note to
   CURRENT_STATE.md, ungated) → read .env + git push violations LAST; orientation_bytes vs
   full-re-read counterfactual from file sizes recorded in the run manifest.
7. **Worktree viability CONFIRMED at HEAD**: boundary e2e (test_omt_harness_e2e.py) is pure
   file-content asserts + uv subprocesses (NO bun/node_modules dependency) → passes in a
   fresh worktree and writes the receipt (receipt ceremony works). test_budget_diet live pin
   (tool_args 2455/2464) is COMMITTED at HEAD (working tree nearly clean: only
   .projects/META.md, WORK.md modified + untracked feature_093 docs/pauses) → the
   harness_repair golden goes green in the worktree after the fix.
8. **Seeded-fault targets re-verified at HEAD**: src/agentx/model/chat/chat_history.py:249
   `ORDER BY timestamp ASC`; UI = src/agentx/ui/screens/chat/chat_controller.py with TWO
   `for msg in messages:` loops (~169 history rebuild, ~180 view) — the view loop is
   disambiguated by a multi-line anchor including `if msg.role != "system":`;
   scripts/omt/harnessc.py:1285 `BUDGET_DIET_PROXIMITY = 64`; utils.py tail
   `    return True`; tests/unit/model/test_chat_history.py present (imports
   `from agentx.model.chat.chat_history import ...` — package installed via uv sync).
9. **API/signature pins for the probe** (fresh reads, exact): runBeforeGatesDry(ctx: GateCtx,
   irOverride?) → GateDecision{gate_id, blocked, msg, skip_ok, fired?, stop?} — dry-run
   CONTINUES after a block (records all blockers) and breaks on stop; EnforcerEnv =
   {client, $, directory, state, safeLog, notify} with createSessionState(); createPhaseTools
   /createTddTools take the shared env (tool() from @opencode-ai/plugin, execute(args,
   {sessionID})); plugin defaults (omt_status/think/nav/kb/net) take {directory, worktree} →
   .tool.omt_*; omt_net runs `uv run scripts/omt/net_check.py` via execFileSync cwd=repoRoot
   (NOT env.$); before-hook args on OUTPUT, after-hook args on INPUT; relOf resolves
   repo-relative against initOmtShared root; getSearchPath(output) for search scopes;
   opencode.jsonc permission structure confirmed (bash allow uv */bun *…, deny
   git commit|push/python*/pip*/pytest; read deny *.env, *.env.*) — driver parses this
   JSONC for the deny model; probe env must scrub OMT_NET_DIR/OMT_LEDGER_PATH/
   OMT_COORDINATION_ROOT (OMT_LEDGER_PATH is a process-level override that beats the
   injected root — omt_shared TA:48).
10. io_bytes model locked: per step = JSON args bytes + result bytes; spawn stdout capped
    8192 B, reads capped 64 KB (offset/limit honored), sessionBootstrap + TA-digest
    injections counted on the first result per session (real hook behavior); tokens_est =
    io_bytes // 4.

## Immediate next step (resume here)

Write the bench package — every decision is now made (analysis_001 + 12 TA thoughts +
pause_d §Completed + this file; nothing to re-derive):

1. `scripts/omt/bench/__init__.py` + `model.py` (Step/TaskDef dataclasses; kinds
   omt/edit/write/read/search/bash/verify/assert; roles
   work/intervention/recovery/verify/violation; optional gate:/tag annotations; expect
   ok|blocked|pass|fail; edit args {path, old, new} | {path, append} | {path, content};
   read args {path, offset?, limit?}; assert args {step, contains?|rc_zero?|rc_nonzero?|
   ok?|refused?}; validate_task) + `tasks.py` (6 real + 2 fixture task defs per the shapes
   in TA:118–122 + pause_d task table) + `sandbox.py` (real: `git worktree add --detach
   <dir> HEAD` under .sandbox/bench/ + fault seeding (the seed edit makes harnessc.py dirty —
   that IS the g.receipt ceremony to measure) + net-state copy/3-binding injection (driver
   runs under sys.path scripts/omt, net.state load/save like test_net_task_claim_generation
   _pool_bundle) + ledger seeding (resume) + design-doc/stub pre-scaffold (major) + uv
   pre-warm (`uv sync`); fixture: harness.ir.json + nav.index.jsonl + net files copied +
   synthetic src/app.js, tests/test_app.js, runner.js + empty ledger) + `probe.ts`
   (hook-bus replication: ONE shared EnforcerEnv with Bun.$; per step navTrack+kbTrack →
   runBeforeGatesDry(ctx, irOverride?) → blocked ⇒ NO effect + NO after-chain; else apply
   effect → sessionBootstrap/trackRead/injectThoughtsOnRead/lspAfterEdit → runAfterGates;
   omt tools via createPhaseTools/createTddTools/plugin defaults; deny model pre-spawn;
   transcript JSON on stdout via console.log(JSON.stringify)) + `metrics.py` (pure
   accounting per TA:120 + removal_delta: annotation-based savings + per-gate
   violations_slipped) + `report.py` (markdown + stable-schema JSON, assumptions section,
   FINDINGS for two-hats shadow-off etc.) + `cli.py` (list/run/removal/run-all) +
   `scripts/omt/task_cost_benchmark.py` shim.
   ⚠ g.receipt round-robin applies (TA:117): ONE write per file per e2e receipt — write all
   9 files in one round; any fix round needs a fresh boundary e2e first.
2. Canary (`omt_skip{scope:"tests", purpose:"canary"}` after re-declaring phase) then goldens
   `tests/scripts/omt/test_task_cost_benchmark.py` — 8 groups per analysis_001 §Goldens
   (validation, metrics math, removal accounting, report render, deny parsing, bun-gated
   fixture trial, bun-gated removal variants, CLI list).
3. First-numbers real run at pinned HEAD → `bench_first_numbers.{json,md}` under
   `.meta/software_development_process/6.testing/features/feature_093.task_cost_benchmark/`;
   unexpected FP blocks = FINDINGS (friction), not failures.
4. Close out: `uv run scripts/omt/harnessc.py check && build`, full suite, test_report.md +
   impl notes (new_feature.py scaffolders), omt_complete, CURRENT_STATE.md (meta_harness_8),
   WORK.md row flip, archive pause c+d+e.

## Blockers

None. (Session-2 blockers all resolved; session-3 produced zero new blockers.)

## Artifacts to read first (resume order)

1. This file.
2. `.meta/software_development_process/3.analysis/features/feature_093.task_cost_benchmark/analysis_001_benchmark_design.md`
   — IN FULL incl. all 12 TA: thoughts (111–122; 117 = g.receipt correction, 118 = net
   sidecar for all sandboxes + concurrent setup, 119 = concurrent step order, 120 = refusal
   unification + fixture read-first, 121 = major-task shape, 122 = resume-task shape).
3. `.sandbox/pause_2026-09-13d.md` §Completed 1–9 (hook-bus contracts, trial-runner
   mechanism, tool surfaces, two-hats deadlock, cost shapes, deny model — still canonical).
4. Reference patterns (only if re-verification needed): `tests/scripts/omt/test_resume_digest.py`
   (_probe_template/_seeded_records + GOTCHA_PROBE_SERIALIZATION),
   `.opencode/plugins/omt_enforcer.ts` + `.opencode/lib/enforcer/*` (gate_driver/
   session_state/nav_gate/phase_gate/tdd_hats/receipt_guard/policy_decision),
   `tests/scripts/omt/test_net_task_claim_generation.py` (_pool_bundle),
   `tests/scripts/omt/test_omt_harness_e2e.py` (receipt mechanics), `scripts/omt/net/gate.py`
   (fire-receipt semantics).

## Idempotency

This pause is the current resumption artifact for T3-4. Re-running the pause workflow on an
unchanged session state must NOT create pause_2026-09-13f — update THIS file in place
(and only bump the letter after substantive new work).

## Result

Paused at bench-package implementation start, round 3 — recon + design now FULLY locked
(includes the g.receipt/round-robin correction and the concurrent-task ordering fix).
Resume by reading this file → analysis_001 (+ all 12 TA thoughts) → write
`scripts/omt/bench/` package.

**WORK.md updated 2026-09-13** (user-confirmed; `[~]` row now points here, superseding
pause_2026-09-13d.md; CURRENT_STATE.md session entry logged) — session is safe to close.
On resume: re-declare `omt_phase{task_type:"minor_feature", phase:"Programming",
feature:"feature_093.task_cost_benchmark", scope:"Implement bench package
(model/tasks/sandbox/probe.ts/metrics/report/cli) + shim + goldens; then first-numbers real
run + artifacts."}` (the active phase record expires 8h), then continue at "Immediate next
step" above.
