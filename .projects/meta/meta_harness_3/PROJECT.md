# PROJECT: meta_harness_3 — Feature-Scoped Lifecycle Gating (v3)

> Status: **complete** · v1.2 (2026-08-16) — v1.0 created the project home from the real-session evaluation `.sandbox/session_2026-08-15_feature_027_completion.md` (a ~90-minute end-to-end OMT++ run that drove feature_027.rag_v2 from "~80% GREEN, 6 failing tests" to `omt_tdd{op:done}` + `omt_complete{Done}`, 31/31 v2 tests green). v1.0 locks the headline principle — **feature-scoped gating beats repo-scoped gating** — and maps the evaluation's 12 friction items + 12 prioritized improvements (§5 table) onto a Phase-A/B/C build surface, each item grounded against the *current* working tree (verified 2026-08-16: `gates.py`, `state.py`, `phase_gate.ts` line anchors below). **v1.2 (2026-08-16): unified re-scope approved by the user** — the project now carries TWO axes: the v3 feature-scoped-gating friction surface (12 items, thesis unchanged) + a measured token-efficiency axis (T1–T4) grounded in opencode.db (1009 agentx sessions, 2026-04-19→2026-08-16: 494M input / 1.07B cache-read tokens, 68.8MB tool-result bytes). Placement: T1 `omt_q{op:state}` summary projection → Phase-A, T2 nav-answer caps → Phase-B, T3 duplicate-read mitigation → Phase-C, T4 schema-budget zero-sum → Decisions (D9). Evidence + per-item measurements in §Token evidence (v1.2). **Phase-A SHIPPED as `feature_028.feature_scoped_gating` (DONE 2026-08-16, test report @ `6.testing/features/feature_028.feature_scoped_gating/test_report.md`); Phase-B/C remain pending as separate features (§Status).
>
> Project home convention: `PROJECT.md` (canonical) + `CURRENT_STATE.md` (session log + resume point), per `.meta/META_HARNESS.omt:184`/`:208`. Non-gated (`.projects/` not in `harness_paths`) — the edits that created this file required no `omt_phase`.

---

## New Session Quick Start

> One line: feature-scoped lifecycle gating (v3) + the token-efficiency axis — Phase-A shipped as feature_028 (DONE 2026-08-16: feature-scoped TDD state, coverage-on-diff, done split, op=state summary 44KB→2.7KB); Phase-B (P2 ergonomics + T2 nav caps) and Phase-C (P3 hygiene + T3 resume digest) pending as separate features.

**Next:** Phase-B build — scaffold as its own feature when called (§Scope Phase-B + CURRENT_STATE.md latest entry).

---

## Summary (one line)

**v3 is the "feature-scoped gating" refactor of the meta harness's lifecycle gates** — the single dominant friction theme the feature_027 post-mortem isolated: three design choices (`omt_phase` resetting in-flight TDD cycles, `omt_tdd{op:done}` gating on the FULL suite, the coverage gate re-opening prior features' untested surface) all share the shape *"feature-scoped action gated by repo-scoped state."* Phase-A ships the three P1 fixes (idempotent `omt_phase`, `feature_suite_passes` + `repo_hygiene_passes` split, coverage-on-diff); Phase-B ships the P2 ergonomics (`omt_tdd{op:sync}`, structural-pin convention, absolute-date lint, skip-scope alignment); Phase-C ships the P3 signal hygiene (two-hats message, g.kb recent-read, artifact scaffolds, LSP allowlist, receipt auto-refresh). Everything is grounded in the evaluation's §5 improvement table and verified against the current working tree — not a redesign of v1/v2 mechanics, an *un-entangling* of the gates that make multi-session major features tax-expensive. v1.2 adds a second, measured axis: **token efficiency** — a deep analysis of the harness's real session costs (opencode.db, 1009 sessions) isolated three token findings (T1–T3) that merge into the same Phase-A/B/C surface, plus one standing policy (T4). No new features: the token items slim EXISTING outputs (D8).

---

## Vision + standing principle + main objectives

**Standing principle (non-negotiable): feature-scoped gating beats repo-scoped gating.**

The evaluation's headline (§5, §6): *"the three P1 items are the workflow-evolution signal — they all share a common shape: **feature-scoped gating beats repo-scoped gating**. The phase lifecycle should gate on the FEATURE's content + hygiene, not on the union of all prior features' drift."* This is the v3 thesis. The harness's protective layers are mature (the evaluation's §2 lists 8 protections that caught real mistakes — resume discipline, node-granularity gotcha, think-gate consult, REFACTOR auto-revert, `harnessc check` hygiene budget, genuine-RED verification, the `omt_q` interrogative layer, two-hats enforcement). v3 does NOT touch those protections. It removes the *entanglement* that makes every feature's lifecycle depend on the union of all prior features' drift — the per-session tax that "discourages multi-session major features and makes any new ship blockable by any random prior drift."

**Main objectives — the project is justified by three outcomes:**

- **(a) Multi-session major-feature continuity — resume must be zero-tax.** Today, resuming a mid-flight feature hits three compounding costs: `omt_phase` silently resets the in-flight TDD cycle (§3.1, verified root cause in `state.py:157-169` + `get_tdd_state`), stranded-red closure is manual busywork (§3.2), and the phase-exit coverage gate re-opens prior finalized features' untested surface (§3.8). v3 makes "resume mid-GREEN" as cheap as "start fresh."
- **(b) Feature-done decoupled from repo-done — no more worldwide-drift triage per ship.** Today `omt_tdd{op:done}` runs the FULL suite via `run_full_suite` (`state.py:257-272`) and `cmd_validate_exit` (`gates.py:156-212`) blocks on any prior feature's drift — the evaluation calls this "the single worst design smell in the session" (§3.6). v3 splits `suite_passes` into `feature_suite_passes` + `repo_hygiene_passes` so a feature ships green-on-its-own-content and clean-hygiene, and prior drift is a separate repo-level triage concern.
- **(c) Preserve every protection that earned its keep — v3 removes friction, not safety.** The evaluation's §2 verdict: *"the harness's protections are NOT theoretical — at least 4 of them caught real mistakes or saved concrete rediscovery this session alone."* None of the §5 improvements removes a gate; each one either narrows a gate's scope (P1), adds an ergonomic op or convention (P2), or fixes a message/one-time cost (P3). The REFACTOR auto-revert guard, genuine-RED refusal, two-hats enforcement, and `harnessc check` hygiene budget all stay byte-identical.
- **(d) Token efficiency — pay for signal, not repetition (v1.2).** Measured against opencode.db (1009 agentx sessions): the harness's small costs are already lean (ceremony tools 257–369B/call; bootstrap tips once-per-session ~690B; schemas 1,094/1,280 budget) but three leaks are real — `omt_q{op:state}` dumps 24–36KB/call (T1), `omt_nav` answers uncapped at 4.5KB avg (T2), and context-compaction-driven duplicate re-reads (T3 — read = 66% of all tool-result bytes, worst case the same file 29× in one session). v1.2 merges T1–T3 into the build phases; T4 (schema budget at cap) becomes standing policy (D9). **Verification anchor (d):** T1 is verifiable by a golden asserting the default `state` envelope ≤ ~2KB (from 24–36KB measured) with `verbose:true` restoring the byte-identical full dump.

**Verification anchor:** (a) is verifiable per-fix — the P1 `omt_phase` idempotency fix is verifiable by re-running the exact §3.1 scenario (declare `omt_phase{phase:Programming, tdd:true}` on a feature with an in-flight GREEN/stranded-red cycle → assert TDD state preserved, not reset to `testlist`); (b) is verifiable by `omt_tdd{op:done}` succeeding while a synthetic prior-feature drift is present (feature's own tests green + hygiene clean → done; prior drift → repo-level triage note, not a block); (c) is verifiable by the P1/P2/P3 surface shipping zero gate removals — the e2e receipt round and the full regression suite (1223 baseline) stay green.

---

## Purpose

### What this project is

A **targeted, evidence-driven improvement of the meta harness's lifecycle-gate architecture**, derived from one real end-to-end session evaluation rather than from theory. The evidence base is `.sandbox/session_2026-08-15_feature_027_completion.md` — a post-mortem written *from inside the loop* by the agent that ran feature_027, with a bias disclaimer, classifying each friction item by scale impact (**COMPOUNDS** = hits every feature/session-resume, **FIXED** = one-time per developer, **LOCAL** = this session only) and prioritizing improvements by scale × cost (§5 table: 3×P1, 4×P2, 5×P3).

v3's job is to implement that table against the current working tree. The build surface is:

| Pri | Improvement (from §5) | Addresses | Verified anchor (2026-08-16) | Phase |
|---|---|---|---|---|
| P1 | `omt_phase` idempotent on in-flight features — **via feature-scoped TDD state derivation** (preserve TDD state; "warn before reset" is the fallback band-aid, not the primary fix) | §3.1, §4.3 | `state.py:157-169` `get_session_records` is **session-scoped** — a new session's phase record makes `mine` non-empty, shadowing the prior session's TDD records → `get_tdd_state` (`state.py:181-192`) returns `testlist`. **Key precedent:** `get_tdd_cycles(feature)` (`state.py:203-205`) is ALREADY feature-scoped (reads all records for `kind=="tdd" and feature==feature`); the fix makes `get_tdd_state`/`get_tdd_mode`/`get_current_test_node` derive feature-scoped too (resolve active feature from the latest phase record). See §Plan review R3 | A |
| P1 | Split `suite_passes` into `feature_suite_passes` + `repo_hygiene_passes`; gate feature-done on the former only — **with a pre-feature baseline so real regressions still block** (R4) | §3.6, §4.4 | **Anchor correction (R1):** the evaluation §3.6 says `cmd_validate_exit` (gates.py:156) runs the full suite — WRONG. Verified: `cmd_validate_exit` (`gates.py:156-212`) checks ONLY dangling reds + coverage gaps; the full-suite run is in `cmd_done` (`cli.py:234` `run_full_suite` + `cli.py:243` `suite_clean` + `state.py:132` `KNOWN_SUITE_FAILURES`). `omt_tdd{op:done}` (cmd_done) and `omt_complete`→validate-exit (cmd_validate_exit) are TWO DISTINCT exit gates; P1-2 targets the former, P1-3 targets the latter | A |
| P1 | Scope coverage-gate (validate-exit) to methods **added** by THIS feature's diff (use `diff_snapshots`); "modified" requires a body-hash extension — see R5 | §3.8 | `gates.py:185-197` coverage scan over ALL public methods of full files; `gates.py:129-131` `diff_snapshots` ALREADY used in the GREEN-hat after-edit. **Two gaps (R5):** (a) `diff_snapshots` (`state.py:232-237`) compares method-name SETS → detects ADDED only, NOT modified; (b) its baseline is the LAST `cmd_green` snapshot (rolling), not a feature-baseline — validate-exit needs a snapshot captured at `omt_phase{phase:Programming}` entry | A |
| P2 | Add `omt_tdd{op:sync}` or auto-promote on detected pass (RED+verified + now-passing → GREEN) | §3.2, §4.2 | `gates.py:173-183` dangling-red derivation (red+verified with no later green at same node) — the stranded nodes the sync op would close | B |
| P2 | Bake the structural-pin-not-count-pin convention; add to recurring gotchas (nav) | §3.9, §4.6 | `test_kb_compiler_build_runs_clean` count pins (feature_027 already re-pinned structural — WORK.md 2026-08-15: "kb count pins → structural kind-pin"); add GOTCHA_ entry | B |
| P2 | Lint for absolute-date literals in temporal-window tests; add to recurring gotchas | §3.10 | `test_u13_op_state_consult_dedup` hardcoded `fresh_ts = "2026-08-09T18:00:00Z"` — feature_027 already fixed dynamically (WORK.md: "u13 hardcoded date → dynamic now-1h"); add the lint + GOTCHA_ | B |
| P2 | Align skip-scope with override-natural scope (coverage-gate → `scope:tests`); OR name the required scope in the gate's error | §3.7, §4.5 | `gates.py:161-171` — `skip_override` honors ONLY `scope:"all"` (line 168); the error path in `phase_gate.ts:296-310` says generic "call omt_skip" | B |
| P3 | Fix two-hats error message for `testlist`/`done` (say "nothing editable," not "only src") — **fold into Phase-A's gates.py round** (R7); consult `HAT_RULES` not state names (R6) | §3.3 | `gates.py:99-107` — `hat` map has `testlist: "planning"`, `done: "complete"`; line 101 ternary flips noun but says "Only src/ edits allowed" when both are blocked. **Robustness (R6):** `HAT_RULES` is IR-derived (`gates.py:84-86`); the fix must check `rules["src"]==False and rules["tests"]==False` rather than hardcode `testlist`/`done`, so it stays correct under IR hat-config changes | A↘ (folded from C per D3) |
| P3 | g.kb consult gate accepts a recent-read exemption — **NEW per-file Read-recency substrate, NOT a mirror of `recent_consults`** (R2) | §3.4 | **Anchor correction (R2):** the plan v1.0 said "mirror `recent_consults`" — imprecise. Verified: `recent_consults` (`omt_q.ts:251-285`) is derived from `think_consult` records (g.think's consult substrate), NOT a KB or Read substrate. The KB gate (`g.kb`, `gate_driver.ts:234`) uses `requires:"session_flag(kb_consulted)"` — a SESSION-LEVEL boolean (`session_state.ts:35` `kb: Map<session,{consulted:boolean}>`), not file-scoped, not recency-scoped. The fix needs a NEW per-file Read-recency record (enforcer after-hook on Read tools writes `file+ts`), OR per-file KB-consult tracking — not a mirror of the think-consult substrate | C |
| P3 | Scaffold commands for phase artifacts (`new_feature.py testing/implementation`) | §3.11 | `scripts/omt/new_feature.py` exists (feature dir scaffold); add `testing`/`implementation` subcommands for the `.meta/software_development_process/<phase>/features/<slug>/` artifact paths | C |
| P3 | Allowlist of known LSP errors; suppressed in post-edit reports | §3.12 | Pre-existing LSP noise cluster (feature_024 virtual-subclass / lazy-import issues recorded in TA: thoughts) — collect + filter | C |
| P3 | Auto-trigger harness-e2e receipt refresh when the receipt-guard trips | §3.5 | `receipt_guard.ts` + `.meta/.omt/omt_harness_e2e_last_run.json` — enforcer hook could auto-refresh instead of block-then-rerun | C |
| P1·T1 | **`omt_q{op:state}` summary projection** — default = counts + top-N; full `decree_health` dump behind `verbose:true` (user-approved shape, 2026-08-16) | §Token T1 | Measured 24–36KB/call (avg 29KB, 8 `state` calls in opencode.db); `omt_q.ts:494-528` builds `decree_health` unconditionally (`slug_variants` 19 entries + full `empty_slug_records` scope payloads); no verbosity knob exists | A |
| P2·T2 | **Nav-answer caps** — size cap + truncation marker + narrowing hint on `omt_nav`/`omt_kb_nav` answers | §Token T2 | Measured `omt_nav` avg 4,543B × 302 calls = 1.37MB; legacy `omt_list_sections` avg 8,077B; no cap in the answer path | B |
| P3·T3 | **Duplicate-read mitigation** — cheapest version: `omt_status` resume-digest so a post-compaction agent re-reads one small digest instead of re-reading big docs N× | §Token T3 | Measured: same file re-read 29×/26×/24× within single sessions (meta_harness_2/PROJECT.md 26× ≈ 520KB in one session); read = 66% of all tool-result bytes (45.5MB of 68.8MB); root cause = context-compaction eviction | C |

### What this project is **not**

- **Not a rewrite of v1/v2 mechanics.** The 9 gates + enforcer + TDD engine + IR compiler + `omt_q` interrogative layer stay. v3 changes *scope semantics* of existing gates (`suite_passes` → feature-scoped, coverage → diff-scoped) and adds *ops/conventions* (`omt_tdd{op:sync}`, structural-pin gotcha, date lint) — no gate is removed, no substrate is replaced.
- **Not a fix to the agent's misuse patterns.** The evaluation's LOCAL items that were the agent's own fault (e.g., not calling `omt_kb_nav` proactively before reading a file) are addressed only where a harness-side change removes the *recurrence* (P3 g.kb recent-read), not by re-architecting.
- **Not the feature_001/002 pending work.** Those remain scope-unset in WORK.md; v3 is a meta-harness project, home-owned in `.projects/meta/meta_harness_3/`, with Phase-A already shipped as feature_028 (see §Status).
- **Not a documentation exercise.** Every P1/P2 item is tied to a verified code anchor + a testable verification shape (see §Scope → Success criteria). The doc is the plan; the gates are the deliverable.

---

## Scope & success criteria

> The P1/P2/P3 table in §Purpose is the locked improvement surface (boundary locked here; exact pytest node IDs and implementation details sharpen at design time). Each item below carries its verified anchor + its testable success shape. (v1.2: surface extended with the T1–T3 token rows — same lock rules, measured anchors in §Token evidence.)

### Phase-A (the three P1 items — the "feature-scoped gating" core — + T1 token item, v1.2)

1. **`omt_phase` idempotent on in-flight features — via feature-scoped TDD state derivation.** Declaring `omt_phase` for a feature that already has an in-flight TDD cycle (within the unlock window) must PRESERVE the TDD state machine instead of resetting it to `testlist`. **Reframe (R3):** `omt_phase` is already idempotent at the ledger level (it appends a phase record); the reset is EMERGENT from `get_session_records` being **session-scoped**. The v3-consistent fix is to make TDD state derivation **feature-scoped** — the same thesis as P1-2/P1-3. `get_tdd_cycles(feature)` (`state.py:203`) is the existing precedent (reads all records for the feature, not session-filtered); `get_tdd_state`/`get_tdd_mode`/`get_current_test_node` should follow suit, resolving the active feature from the latest phase record (no `gate`/`after-edit` CLI change needed — they already pass `session`, which resolves to one active feature per session, matching the TS-side `getActiveFeaturePhase` model). The "warn before reset" option from the evaluation is a fallback band-aid, not the primary fix (it makes the reset visible without removing the entanglement). Anchor: `state.py:157-169` (`get_session_records`) + `state.py:181-192` (`get_tdd_state`) + `state.py:203-205` (`get_tdd_cycles` — the feature-scoped precedent). Success: re-run the §3.1 scenario — hermetic ledger with TWO session ids (prior session writes red+green records; new session writes only a phase record), declare `omt_phase{phase:Programming, tdd:true}` → assert TDD state shows the prior cycles (`cycles > 0`, stranded_red preserved), NOT `testlist`/`cycles: 0`.
2. **`suite_passes` → `feature_suite_passes` + `repo_hygiene_passes` — WITH a pre-feature baseline so real regressions still block (R4).** `omt_tdd{op:done}` gates on the feature's OWN test dir green + `harnessc check` hygiene clean + `KNOWN_SUITE_FAILURES` allowlist; full-suite drift of PRIOR features becomes a repo-level triage note, not a block. **Safety-critical addition (R4):** a naive split (feature's test dir only) would let a feature that BREAKS a prior feature's test (real regression) ship green — a protection regression that violates D5. The fix MUST distinguish DRIFT (prior test was already failing pre-feature) from REGRESSION (prior test was passing pre-feature, now failing). Mechanism: snapshot `baseline_failures` (the set of failing node IDs) at `omt_phase{phase:Programming}` entry, store in the phase ledger record; at `cmd_done`, `regressions = current_failures − baseline_failures − KNOWN_SUITE_FAILURES`. Regressions block `done`; drift does not. This mirrors the existing `KNOWN_SUITE_FAILURES` pattern (per-feature-baselined instead of global-hardcoded) and is the harness-consistent way to decouple without losing the regression catch. Anchor: `cli.py:233-243` (`cmd_done` + `run_full_suite` + `suite_clean`) + `state.py:132` (`KNOWN_SUITE_FAILURES`) + `phase_gate.ts:204-208` (phase record — add `baseline_failures` field). Success: `omt_tdd{op:done}` succeeds while a synthetic prior-feature DRIFT is present (prior test broken at baseline AND still broken → drift note, not a block); the SAME `done` BLOCKS when a prior test that was GREEN at baseline is now RED (regression) — the regression golden is the D5 guard.
3. **Coverage-gate scoped to THIS feature's diff (ADDED methods; MODIFIED needs a body-hash extension — R5).** `cmd_validate_exit`'s coverage scan checks only methods ADDED by this feature's diff, not ALL public methods of files the feature touched additively. **Two design decisions to lock at design time (R5):** (a) **Scope honesty** — the existing `diff_snapshots` (`state.py:232-237`) compares method-name SETS (`(class, method)` pairs) → detects ADDED methods only, NOT modified ones (same name, changed body). The plan v1.0 said "added/modified" using a mechanism that only does "added." Either scope P1-3 to "added methods" (matches existing mechanism, cheaper, honest) OR extend `extract_public_methods` (`ast_checks.py:89-114`) with a body-hash/line-range field so `diff_snapshots` detects modifications (more correct, more work). Recommendation: ship "added" in Phase-A (matches the GREEN-hat precedent), defer "modified" to a follow-up unless a Phase-A scenario demands it. (b) **Feature-baseline snapshot** — the per-edit `diff_snapshots` baseline is the LAST `cmd_green` snapshot (rolling, overwritten at `cli.py:174`), NOT the feature-baseline. At validate-exit, `load_snapshot` returns last-GREEN, not feature-start. To scope coverage to "methods added by THIS feature," capture a feature-baseline snapshot set at `omt_phase{phase:Programming}` entry (or first RED) — store per-feature alongside the existing `SNAPSHOT_DIR` — and diff against it at validate-exit. This is an EXTENSION of the snapshot substrate (a new feature-baseline tier), not a literal "promote the existing mechanism." Anchor: `gates.py:185-197` (full-file `find_untested_methods`) + `gates.py:129-131` (GREEN-hat `diff_snapshots` precedent) + `state.py:232-237` (`diff_snapshots` — added-only) + `ast_checks.py:89-114` (`extract_public_methods` — where a body-hash would go). Success: additive edit to a file with pre-existing untested methods (the §3.8 feature_024 surface) exits Testing WITHOUT requiring a `scope:all` skip; a NEW method added by the feature without a test reference still blocks.

T1. **`omt_q{op:state}` summary projection (v1.2 token item, user-approved shape: summary default + full flag).** The default `state` envelope returns counts + top-N (e.g., `slug_variants_count`, first 3 `empty_slug_records` with truncated `scope`); `verbose:true` restores the byte-identical full dump. Read-only op — no gate semantics touched (D8). Anchor: `omt_q.ts:494-528` (`decree_health` build + envelope assembly) + `omt_q.ts:164-171` (the `decree_health` type). Success: golden asserts the default envelope ≤ ~2KB (from 24–36KB measured) AND `verbose:true` restores the full dump byte-identical. **Receipt note:** `omt_q.ts` is harness surface — T1 appends to the R9 order as an independent 5th round (no dependency on state.py/gates.py/cli.py/phase_gate.ts rounds).

### Phase-B (the P2 items — multi-session TDD comfort + test-design conventions — + T2 token item, v1.2)

4. **`omt_tdd{op:sync}` (or auto-promote on detected pass).** A RED+verified node whose test now passes promotes to GREEN without a fresh red→green dance; `omt_q{op:state}`'s `stranded_red` becomes actionable. **Location (R8):** the §5 table says "low (gates.py)" — imprecise. The op is a NEW subcommand in `cli.py` (`cmd_sync` + dispatch in `main()`'s `commands` dict), reusing the dangling-red derivation from `gates.py:173-183` (`cmd_validate_exit`) and writing a `green` ledger record via `state.py:write_ledger`. **Two-hats note (R8):** sync writes a `green` record (state mutation) — it must require the same test-passes condition as `cmd_green` (`cli.py:155-189`) and be a no-op (with a clear message) if the test still fails, so it cannot fabricate a GREEN. Anchor: `gates.py:173-183` (dangling-red detection, reused) + `cli.py:330-393` (`main` dispatch — where `sync` registers) + `state.py:102-106` (`write_ledger`). Success: the §3.2 scenario — 3 stranded nodes declared RED in a prior session, now passing → one `op:sync` call closes all 3 (or each auto-promotes on detected pass), zero manual `omt_tdd{op:green}` calls; a sync at a node whose test still fails returns `ok:false` and writes nothing.
5. **Structural-pin-not-count-pin convention + GOTCHA_ entry.** Document "magic-number pins on AST-scraped counts are smell; pin kinds + presence + artifact files" in the recurring-gotchas index (`omt_nav{query:"GOTCHA_"}`). Anchor: `test_kb_compiler_build_runs_clean` (already re-pinned structurally in feature_027). Success: the GOTCHA_ entry exists and is nav-queryable; no new count-pin lands in `tests/` (enforced by review, not a gate).
6. **Absolute-date lint for temporal-window tests + GOTCHA_ entry.** Flag `ts = "20\d\d-\d\d-\d\dT"` literals in test files as date-drift candidates; advertise the `now - timedelta` pattern. Anchor: `test_u13_op_state_consult_dedup` (fixed dynamically in feature_027). Success: the lint command exists and flags a planted absolute-date literal; the GOTCHA_ entry is nav-queryable.
7. **Skip-scope aligned with override-natural scope.** The coverage-gate override honors `scope:tests` (its natural scope) — or the gate's error message names the required scope explicitly (`call omt_skip{scope:'all', ...}`). Anchor: `gates.py:161-171` (`skip_override` honors only `"all"`) + `phase_gate.ts:296-310` (generic error). Success: `omt_skip{scope:"tests"}` satisfies the coverage-gate override (or the error message states the exact required call verbatim).

T2. **Nav-answer caps (v1.2 token item).** `omt_nav`/`omt_kb_nav` answers get a size cap; a truncated answer ends with an explicit truncation marker + narrowing hint (e.g., "…truncated at N bytes — narrow with file:/tag_type:"). Anchor: `omt_nav.ts` op impls (`op=nav` :200, `list_sections` :276, `cross_ref` :313, `quick_ref` :345) + the `include_context` flag already present. Success: golden asserts the cap + marker on a synthetic wide query; answers under the cap are byte-identical to today.

### Phase-C (the P3 items — signal hygiene + one-time costs — + T3 token item, v1.2)

8. **Two-hats error message for `testlist`/`done` — folded into Phase-A's gates.py round (R7); consult `HAT_RULES` not state names (R6).** When both src and tests are blocked, the message says "nothing editable — declare `omt_tdd{op:red}` at a failing test to enter the test hat" instead of "Only src/ edits allowed." **Robustness (R6):** the fix must test `rules["src"]==False and rules["tests"]==False` (the IR-derived `HAT_RULES` at `gates.py:84-86`) rather than hardcode `state in ("testlist","done")`, so it stays correct if the IR hat config changes a state's allow-set. Anchor: `gates.py:99-107`. Success: golden test asserts the exact "nothing editable" message for `testlist` and `done` states, AND a golden with a synthetic IR hat config (a state with both blocked) emits the same message — proving the fix is config-driven, not state-name-driven.
9. **g.kb recent-read exemption — NEW per-file Read-recency substrate, not a mirror of `recent_consults` (R2).** If the file was Read in the current session (or within N minutes), the KB consult gate considers the consult satisfied for THAT file. **Anchor correction (R2):** v1.0 said "mirror the `recent_consults` substrate" — imprecise. Verified: `recent_consults` (`omt_q.ts:251-285`) folds `think_consult` records (g.think's substrate), not KB or Read events; the KB gate (`g.kb`, `gate_driver.ts:234`) uses `requires:"session_flag(kb_consulted)"` — a SESSION-LEVEL boolean (`session_state.ts:35`). The fix requires a NEW per-file Read-recency record (enforcer after-hook on Read tools writes `{file, ts}` to the ledger or session-state), consulted by the `g.kb` predicate per-file. Note the current session-boolean already makes `g.kb` a once-per-session cost (it stops firing after the first `omt_kb_nav`) — so P3-9 is lower-leverage than v1.0 implied; the §3.4 event was the first-src-edit-before-any-consult case. Success: read-file-then-edit in the same turn does not re-fire `omt_kb_nav` (the recent Read satisfies the gate for that file); a blind edit to a never-read file still blocks.
10. **Phase-artifact scaffolds.** `uv run scripts/omt/new_feature.py testing --feature <slug>` / `implementation --feature <slug>` create the correct `.meta/software_development_process/<phase>/features/<slug>/` path + a stub. Success: the §3.11 mis-creation (repo-root vs `.meta/` root) becomes impossible for the scaffolded phases.
11. **Known-LSP-error allowlist.** Collect the pre-existing LSP noise cluster (feature_024 virtual-subclass / lazy-import issues, recorded in TA: thoughts) into an allowlist; post-edit reports suppress allowlisted entries and surface only NEW errors. Success: the pre-existing 4–9 errors on `main_controller.py` / `tui/provider.py` no longer appear in post-edit reports; a planted new error does.
12. **Receipt auto-refresh.** When the receipt-guard trips mid-logical-fix, the enforcer auto-triggers the e2e receipt refresh and proceeds (rather than block-then-rerun). Success: a second harness-surface edit in one round no longer blocks — the receipt refreshes automatically (guarded so a FAILING refresh still blocks).

T3. **Duplicate-read mitigation (v1.2 token item; design-needed).** Measured: the same file re-read up to 29×/26×/24× within single sessions (root cause: context-compaction eviction forces the agent to re-read). Cheapest harness-side version: an `omt_status` resume-digest (≤ ~2KB) so a post-compaction agent re-orients from one small read instead of N full-doc re-reads. Exact mechanism fixed at Phase-C design. Success: shape fixed at design (candidate: a simulated post-compaction resume costs one digest read ≤ ~2KB instead of re-reading PROJECT.md/docs at full size).

### Out of scope (explicit non-goals)

- **No gate removals.** Every P1/P2/P3 item keeps or narrows a gate's scope; the protection surface (§2's 8 protections) stays byte-identical.
- **No new substrate.** v3 uses the existing ledger/IR/snapshot/`diff_snapshots`/`recent_consults` mechanisms; no new state store, no new IR fields (beyond any minimal `suite_passes` split fields in the done-checklist envelope).
- **No change to the §12 phase-artifact matrix or the phase FSM.** `omt_phase` idempotency preserves the FSM; it does not add phases.
- **No `omt_q` new ops beyond the P1/P2 needs.** `omt_q{op:state}`'s `stranded_red` may gain a "syncable" flag, but the interrogative layer itself is not re-scoped.
- **No opencode/agent-side changes** (subagent delegation, skills, MCP) — those belong to meta_harness_2's U15/U16 surface, not v3.
- **No fixing the harness's pre-existing test-design debt** beyond the two GOTCHA_ conventions (P2-5, P2-6) — existing count-pins and date literals are fixed opportunistically, not as a v3 deliverable.
- **No new features via the token axis (v1.2 boundary, user-set).** T1–T3 slim EXISTING outputs (projection, cap, digest); they add no tool, no gate, no substrate — the only API delta is T1's `verbose` flag on an existing op.

### Success criteria (testable)

- **Phase-A gates:** each of the three P1 fixes has a golden test reproducing the §3.1/§3.6/§3.8 scenario against a hermetic ledger/snapshot env (`OMT_LEDGER_PATH` / `OMT_SNAPSHOT_DIR` redirect, per `state.py:34-37`) — see the per-item Success shapes above.
- **Phase-B ergonomics:** `op:sync` golden (stranded 3-node close in one call) + the GOTCHA_ entries nav-queryable + the date lint flagging a planted literal + the skip-scope golden.
- **Phase-C signal hygiene:** exact-message golden for the two-hats text + the g.kb recent-read golden + scaffold-path golden + LSP-allowlist golden (planted-new-error surfaces) + receipt-auto-refresh golden.
- **Token targets (v1.2):** T1 — golden asserts the default `omt_q{op:state}` envelope ≤ ~2KB (from 24–36KB measured) AND `verbose:true` restores the full dump byte-identical. T2 — golden asserts the nav-answer cap + truncation marker on a synthetic wide query; under-cap answers byte-identical to today. T3 — success shape fixed at Phase-C design (candidate: post-compaction resume costs one ≤ ~2KB digest read instead of N full-doc re-reads).
- **Full regression:** the 1223-pass baseline (3 known react_screen + 2 feature_016 allowlisted) stays green through every phase; `harnessc check` clean; e2e receipt discipline maintained for every `scripts/omt/` / `.opencode/` / `.meta/*.omt` edit.
- **No protection regression:** the §2 protections are pinned by their existing tests (REFACTOR auto-revert, genuine-RED refusal, two-hats, `harnessc check` budget) — those pins stay green unchanged. **D5 guard (added v1.1, R4):** the P1-2 split MUST ship with the `baseline_failures` regression-detection mechanism so a feature that breaks a prior feature's GREEN test still blocks `done`; the regression golden (prior test GREEN at baseline → RED now → blocks) is a mandatory Phase-A exit criterion. Without it, P1-2 is a protection regression and violates D5.

---

## Plan review (v1.1 — pre-build verification against the working tree)

> This section records the discrepancies + gaps found by re-verifying v1.0's anchors against the current code (2026-08-16, same commit as v1.0) and re-reading the evaluation evidence. Each finding is grounded in a specific file:line. v1.0's thesis (feature-scoped gating) is unchanged and strengthened; the corrections below make the build surface honest + safe. Inline corrections were applied to the §Purpose table + §Scope items; this section is the audit trail + the sequencing/verification sharpening that didn't fit inline.

### Anchor corrections (factual)

- **R1 — P1-2 anchor mislocation (inherited from the evaluation).** The evaluation §3.6 states "`cmd_validate_exit` (gates.py:156) checks `suite_passes=true` which runs the FULL pytest suite." **Verified WRONG:** `cmd_validate_exit` (`gates.py:156-212`) checks ONLY dangling reds (173-183) + coverage gaps (185-197) + skip_override (161-171) — it never runs the suite. The full-suite run is in `cmd_done` (`cli.py:234` `run_full_suite` + `cli.py:243` `suite_clean`). v1.0's anchors happened to point to `cli.py`, but the §Purpose narrative echoed the evaluation's mislocation. **Implication:** `omt_tdd{op:done}` (`cmd_done`, cli.py) and `omt_complete`→validate-exit (`cmd_validate_exit`, gates.py) are TWO DISTINCT exit gates with DIFFERENT checks, called by DIFFERENT tools at DIFFERENT times. P1-2 (suite split) targets `cmd_done`; P1-3 (coverage-on-diff) targets `cmd_validate_exit`. The plan must not conflate them. (The evaluation's §3.6/§3.8 also blur this; v1.1 separates them.)
- **R2 — P3-9 substrate mischaracterization.** v1.0 said "mirror the `recent_consults` substrate (8h window) that g.think's U13 already uses." **Verified imprecise:** `recent_consults` (`omt_q.ts:251-285`) folds `think_consult` records — g.think's consult substrate — NOT KB-consult or Read events. The KB gate (`g.kb`, `gate_driver.ts:234`) uses `requires:"session_flag(kb_consulted)"` — a SESSION-LEVEL boolean (`session_state.ts:35` `kb: Map<session,{consulted:boolean}>`), set true after ANY `omt_kb_nav` call, not file-scoped, not recency-scoped. "Mirror recent_consults" would mislead an implementer to reuse the think-consult substrate. The actual fix needs a NEW per-file Read-recency record. Also: because `g.kb` is session-boolean, it's already a once-per-session cost (stops firing after the first `omt_kb_nav`) — the §3.4 event was the first-src-edit-before-any-consult case, so P3-9 is lower-leverage than v1.0 implied.

### Reframings (thesis consistency)

- **R3 — P1-1 reframe: "feature-scoped TDD state derivation," not "omt_phase idempotency."** v1.0 framed P1-1 as making `omt_phase` idempotent. But `omt_phase` (`phase_gate.ts:204-208`) is ALREADY idempotent at the ledger level — it appends a phase record, nothing resets explicitly. The TDD reset is EMERGENT from `get_session_records` (`state.py:157-169`) being **session-scoped**: a new session's phase record makes `mine` non-empty, shadowing the prior session's TDD records, so `get_tdd_state` (`state.py:181-192`) falls through to `testlist`. The v3-consistent fix is to make TDD state derivation **feature-scoped** — the same thesis as P1-2/P1-3. The precedent already exists: `get_tdd_cycles(feature)` (`state.py:203-205`) reads ALL records for the feature (not session-filtered); `cmd_validate_exit` and `cmd_done` already see cross-session feature history this way. The in-session functions (`get_tdd_state`/`get_tdd_mode`/`get_current_test_node`/`cmd_status`) are the only session-scoped outliers. The fix: resolve the active feature from the latest phase record (one active feature per session, matching the TS-side `getActiveFeaturePhase`), then derive TDD state from feature-scoped records within the unlock window. No `gate`/`after-edit` CLI change needed (they pass `session`, which resolves to the active feature). The "warn before reset" option from the evaluation is a fallback band-aid, not the primary fix.

### Gaps / missing design (safety + correctness)

- **R4 — P1-2 safety regression (the most important finding).** v1.0's P1-2 said "full-suite drift of PRIOR features becomes a repo-level triage note, not a block." But a naive split (feature's test dir only) would let a feature that BREAKS a prior feature's test (a real regression, not drift) ship green — removing the cross-feature regression catch that the current full-suite gate provides. **This violates D5 ("no protection regression").** The fix MUST distinguish DRIFT (prior test was already failing at feature start) from REGRESSION (prior test was passing at feature start, now failing). Mechanism: snapshot `baseline_failures` (set of failing node IDs) at `omt_phase{phase:Programming}` entry → store in the phase ledger record; at `cmd_done`, `regressions = current_failures − baseline_failures − KNOWN_SUITE_FAILURES`. Regressions block; drift doesn't. This mirrors the existing `KNOWN_SUITE_FAILURES` pattern (per-feature-baselined instead of global-hardcoded) and is the harness-consistent decoupling. **Mandatory Phase-A exit criterion:** a regression golden (prior test GREEN at baseline → RED now → `done` blocks).
- **R5 — P1-3 overclaim + baseline gap.** v1.0 said "methods ADDED/MODIFIED by THIS feature's diff (use `diff_snapshots`)." Two issues: (a) `diff_snapshots` (`state.py:232-237`) compares `(class, method)` name SETS → detects ADDED methods only, NOT modified (same name, changed body). The "modified" claim is not supported by the existing mechanism. (b) The per-edit `diff_snapshots` baseline is the LAST `cmd_green` snapshot (rolling, overwritten at `cli.py:174`), not a feature-baseline; at validate-exit `load_snapshot` returns last-GREEN, not feature-start. To scope coverage to "this feature's diff," the plan needs a FEATURE-BASELINE snapshot tier captured at `omt_phase{phase:Programming}` entry (or first RED), stored per-feature, and diffed against at validate-exit. This is an EXTENSION of the snapshot substrate, not a literal "promote the existing mechanism." Recommendation: ship "added methods" in Phase-A (matches the GREEN-hat precedent); defer "modified" unless a Phase-A scenario demands it (would require a body-hash field in `extract_public_methods`).
- **R6 — P3-8 robustness.** `HAT_RULES` is IR-derived (`gates.py:84-86` via `_derive_hat_rules`). The two-hats message fix must test `rules["src"]==False and rules["tests"]==False` (both blocked) rather than hardcode `state in ("testlist","done")`, so it stays correct if the IR hat config changes a state's allow-set. A golden with a synthetic IR hat config (a state with both blocked) proves config-driven behavior.
- **R8 — P2-4 location + state-mutation note.** The §5 table says "low (gates.py)" for `omt_tdd{op:sync}` — imprecise. The op is a NEW subcommand in `cli.py` (`cmd_sync` + dispatch registration), reusing `gates.py:173-183` dangling-red detection and writing a `green` record via `state.py:write_ledger`. Because sync writes a `green` record (state mutation), it must require the same test-passes condition as `cmd_green` and be a no-op if the test still fails — otherwise it could fabricate a GREEN and break the genuine-RED protection (§2.6).

### Sequencing / build-order (Phase-A)

- **R7 — fold P3-8 into Phase-A's gates.py round (D3 commitment).** D3 permits folding trivial same-file P3 items into Phase-A/B rounds; v1.0 left it as "may." v1.1 commits: P3-8 (gates.py:99-107) lands in the SAME gates.py round as P1-3 (gates.py:185-197) — both are harness-surface edits to one file, and the receipt round-robin guard charges ONE e2e refresh per file per round. Folding saves a receipt round and avoids a mid-Phase-A stall. (P3-8 reclassified C→A in the §Purpose table.)
- **R9 — Phase-A receipt-round + dependency order.** Phase-A edits four harness-surface files, each subject to the per-file second-edit receipt guard (one edit per file per e2e receipt; refresh `.meta/.omt/omt_harness_e2e_last_run.json` between rounds):
  1. **`state.py`** (P1-1: feature-scoped `get_tdd_state`/`get_tdd_mode`/`get_current_test_node`) — independent; ship first.
  2. **`gates.py`** (P1-3: coverage-on-diff + feature-baseline diff; P3-8: two-hats message) — fold P3-8 into this round; depends on the feature-baseline snapshot substrate (shared with P1-2's R4 baseline — design these together).
  3. **`cli.py`** (P1-2: `cmd_done` split + `baseline_failures` regression check) — depends on the feature-baseline substrate from round 2.
  4. **`phase_gate.ts`** (P1-1: `omt_phase` records `baseline_failures` at Programming entry; P1-2: done-checklist envelope fields) — the TS side of the baseline capture; pair with round 3.

  Dependency: P1-1 (state.py) is independent. P1-3 (gates.py) and P1-2 (cli.py) SHARE the feature-baseline snapshot substrate — design it once, consume in both. Order: state.py → gates.py (+P3-8) → cli.py → phase_gate.ts. Each round: edit → `uv run pytest tests/scripts/omt/test_omt_harness_e2e.py -q` → refresh receipt → next file.

### Verification sharpening

- **R10 — hermetic-test-dir for P1-2.** D4 specifies hermetic-ledger (`OMT_LEDGER_PATH`/`OMT_SNAPSHOT_DIR` redirects). P1-2's golden ALSO needs a hermetic TEST-DIR redirect (a temp `tests/features/feature_synthetic/` with a planted broken test) so the drift-vs-regression golden never mutates the real suite. The golden plants: (a) a DRIFT test (broken at baseline AND at done → `done` succeeds with a drift note); (b) a REGRESSION test (GREEN at baseline, broken at done → `done` blocks). Both use the same hermetic ledger + temp test dir.
- **R11 — two-session ledger for P1-1.** The P1-1 golden must use a hermetic ledger with TWO distinct session ids: the prior session writes red+green records for the feature; the new session writes ONLY a phase record. Assert that after the new phase record, `get_tdd_state` returns the preserved state (cycles > 0, stranded_red intact), NOT `testlist`. This reproduces the exact §3.1 resume scenario hermetically.

### What v1.1 does NOT change

- The v3 thesis (feature-scoped gating beats repo-scoped gating) is unchanged; R3 strengthens it by applying the thesis to P1-1 consistently (v1.0 framed P1-1 as a separate "idempotency" fix).
- D1–D6 hold; D5 is reinforced by R4 (the regression golden is now a mandatory Phase-A exit criterion).
- The P1/P2/P3 priority ordering holds; only P3-8 is reclassified C→A (folded per D3).
- No new evidence round is required for Phase-A: R1/R2/R3/R5 are code-evident (the anchors show the entanglement mechanically), so n=1 (the feature_027 evaluation) is sufficient. A second session evaluation remains the bar for re-scoping (D1), not for shipping Phase-A.

---

## Token evidence (v1.2 — measured deep analysis, 2026-08-16)

> The v1.2 audit trail. Method: read-only aggregate SQL over `~/.local/share/opencode/opencode.db` (project `b214281b…` = this repo; `session.tokens_*` columns + `part.data` JSON tool-call blobs) + static byte audit of the harness surface. No session content was read — sizes, counts, and distributions only. User approved the unified re-scope + T1's fix shape (summary default + `verbose` flag) on 2026-08-16.

### Measured base (1009 sessions, 2026-04-19 → 2026-08-16)

- Tokens: **494M input / 8.4M output / 1.07B cache-read; cost $294.54**. Avg **26.2 turns/session** (max 363; 26,401 messages total).
- Tool-result bytes: **68.8MB total** — `read` 45.5MB (66%, 7,565 calls, 6.1KB avg), `bash` 13MB (19%), **all `omt_*` combined 3.26MB (4.7%, 2,012 calls)**.

### Verified lean (no action — measured, not assumed)

- Ceremony tools: `omt_phase` 369B, `omt_complete` 296B, `omt_skip` 332B, `omt_tdd` 257B per call; ~1.19 omt-ceremony calls/session.
- Bootstrap tips: ~690B **once per session** (nav_gate.ts S6 single-emission consolidation works — 234 of 2,659 reads in the last 30 days carried tips, i.e., first-tool-result-only as designed).
- Static budgets: AGENTS.md 2,626B vs `@budget agents_md max=2816`; `@tool` descriptions 1,094B vs `tool_schemas max=1280`; five prior diet rounds (OPT-A/B/D/H, improvement002–007) already cut this surface.
- Gate-block messages ~350B (887 errored tool calls lifetime) — the message bytes are fine; the cost is the retry loop, which the P1/P2 friction items already address.

### The three token findings (merged into §Scope)

- **T1 — `omt_q{op:state}` verbosity (avg 29KB/call, range 24–36KB; 8 calls).** Root cause verified in code: `decree_health` (omt_q.ts:494-528) inlines `slug_variants` (19 entries) + full `empty_slug_records` payloads (long `scope` sentences) unconditionally; no verbosity knob (omt_q.ts:164-171). The agent asking "state" needs phase/tdd_position/stranded_red (~500B), not a global ledger health dump. → Phase-A; summary default + `verbose:true` (user-approved).
- **T2 — nav answers uncapped (`omt_nav` avg 4,543B × 302 calls = 1.37MB; legacy `omt_list_sections` avg 8,077B).** → Phase-B; cap + truncation marker + narrowing hint.
- **T3 — duplicate in-session re-reads (read = 66% of tool bytes; worst measured: same file 29× in one session; meta_harness_2/PROJECT.md 26× ≈ 520KB in one session).** Root cause: context-compaction eviction, not harness behavior — harness leverage is indirect (resume-digest). → Phase-C; design-needed.
- **T4 — schema budget at cap (1,094/1,280 after omt_q).** Not a leak — a zero-sum constraint. → D9 standing policy.

### What v1.2 does NOT change

- The v3 thesis + all 12 friction items + R1–R11 hold; Phase-A build order (R9) is unchanged except T1's independent 5th round (`omt_q.ts`).
- D5 unchanged for gates/protections; D8 (new) covers output slimming.
- The analysis consumed ~0 build budget: evidence is DB aggregates + file sizes, no harness edits were made.

---

## Status

- [x] Project home created — `.projects/meta/meta_harness_3/` (`PROJECT.md` v1.0 + `CURRENT_STATE.md` iter-0)
- [x] Evidence base read + verified — `.sandbox/session_2026-08-15_feature_027_completion.md` §2/§3/§4/§5/§6; line anchors re-verified against the current working tree (2026-08-16)
- [x] Improvement surface locked — P1/P2/P3 table (12 items) with verified anchors (v1.0)
- [x] **Plan review v1.1 — pre-build verification against the working tree** (2026-08-16): 11 findings (R1–R11) — 2 anchor corrections (P1-2 cmd_done vs cmd_validate_exit; P3-9 recent_consults is think-substrate not KB/Read), 1 reframe (P1-1 → feature-scoped TDD state derivation), 1 safety-critical gap (P1-2 `baseline_failures` regression guard, D5), 2 correctness gaps (P1-3 added-vs-modified + feature-baseline snapshot tier; P3-8 HAT_RULES robustness), 1 fold (P3-8 → Phase-A gates.py round), 2 location notes (P2-4 cli.py), sequencing (R9 receipt-round order) + verification sharpening (R10/R11 hermetic fixtures). Inline corrections applied to §Purpose table + §Scope; audit trail in §Plan review. See §Iteration log iter v1.1.
- [x] **Token deep analysis + unified re-scope (v1.2)** (2026-08-16): opencode.db evidence (1009 sessions; 494M input tokens; 68.8MB tool-result bytes), 4 token findings (T1–T4), user-approved placement (T1→Phase-A as 5th R9 round, T2→Phase-B, T3→Phase-C, T4→D9 policy) + user-approved T1 shape (summary default + `verbose:true`). See §Token evidence (v1.2) + iter v1.2.
- [x] Feature dir scaffolded — **feature_028.feature_scoped_gating** (2026-08-16; TDD refactor, `omt_phase` declared)
- [x] Phase declared — `omt_phase` Programming/TDD for feature_028 (2026-08-16)
- [x] **Phase-A build (P1 ×3 + folded P3-8 + T1) — DONE 2026-08-16** (feature_028, 5 R9 receipt rounds: state.py P1-1 → gates.py P1-3+P3-8 → cli.py P1-2+R4 → phase_gate.ts R4 capture → omt_q.ts T1). 10/10 testlist behaviors GREEN; `omt_tdd{op:done}` ✅; D5 regression golden shipped per R4. Test report @ `6.testing/features/feature_028.feature_scoped_gating/test_report.md`
- [x] **Phase-A post-ship verification (2026-08-16, independent review)** — 217/217 `tests/scripts/omt/`; full suite 1277/1277 (0 failures; the 2 KNOWN_SUITE_FAILURES tolerated at ship time pass in-process); `harnessc check` 0 errors (247 records); live `omt_q{op:state}` = compact T1 summary (2,706B); `omt_q{op:drift}` → `drift_records: []`
- [ ] Phase-B build (P2 ×4 + T2) — pending (separate feature per the v1.2 surface; not scaffolded)
- [ ] Phase-C build (P3 ×4 — P3-8 folded to A + T3) — pending (separate feature per the v1.2 surface; not scaffolded)

---

## Out-of-scope reminders (deferred, not done by this document)

- Feature-dir scaffolding — not created by this document (awaited project-definition approval at writing time); **since completed: feature_028.feature_scoped_gating, 2026-08-16**.
- Phase declaration — no `omt_phase` was invoked by this document; **the first decree was declared post-approval (feature_028 Programming/TDD, 2026-08-16)**.
- `src/` edits — none; this document is project-home markdown only.
- Any design/analysis/test artifacts — owned by the feature_028 phases, not by this project home.

---

## Decisions log (locked — do not re-litigate without new evidence)

- **D1 — v3 is a feature-scoped-gating refactor, not a v1/v2 rewrite.** The evaluation's §5 headline (feature-scoped gating beats repo-scoped gating) is the v3 thesis; the P1/P2/P3 table is the locked improvement surface. A new evidence round (a second session evaluation, or a Phase-A build revealing a different dominant friction) is required before re-scoping.
- **D2 — Project slug lives in `.projects/meta/meta_harness_3/`** (underscore convention, matching `meta_harness_2` + `rag_v2`). The companion feature slug is the next free slot **feature_028** (verified 2026-08-16: 027 = rag_v2); exact feature_028 slug chosen at scaffold time.
- **D3 — The P1 items are Phase-A, P2 Phase-B, P3 Phase-C** (per the evaluation's §5 scale × cost ordering). P3 items that are trivial one-liners (P3-8 two-hats message) may be folded into Phase-A/B rounds if their edit lands in the same file — a build-order decision, not a scope change.
- **D4 — Verification is hermetic-ledger + golden-test, not live-suite-only.** P1/P2 gates are tested via `OMT_LEDGER_PATH` / `OMT_SNAPSHOT_DIR` redirects so the golden scenarios never mutate the real ledger; full-suite regression runs at phase exit per the §12 matrix.
- **D5 — No gate removals; no protection regression.** The §2 protections stay byte-identical; their existing pin tests are the regression guard. A P1/P2 item that would require weakening a protection must be re-scoped, not shipped.
- **D6 — The g.kb recent-read exemption (P3-9) mirrors the existing `recent_consults` substrate** (8h window, already used by g.think's U13) — it does not invent a new consult-recency mechanism. **(Superseded by R2 at v1.1 — `recent_consults` is the think-consult substrate, not KB/Read; the P3-9 fix needs a NEW per-file Read-recency substrate. D6 kept for audit trail.)**
- **D7 — Unified re-scope (v1.2, user-approved 2026-08-16): the project carries two axes** — feature-scoped-gating friction (12 items) + measured token efficiency (T1–T3). One unified surface prioritized by scale × cost; the v3 thesis is unchanged; "no new features" bounds the token axis (T-items slim existing outputs only).
- **D8 — Slimming is not removal (v1.2, user-approved):** tool-output projections, answer caps, and schema text MAY be slimmed/consolidated when the protection behavior is provably unchanged (existing pin tests stay green). D5 still governs gates/protections byte-identically.
- **D9 — Schema budget is zero-sum (T4 policy):** `tool_schemas max=1280` sits at 1,094 after omt_q pushed it to the cap — any future tool description must displace existing bytes (OPT-H 18→7 consolidation precedent), not raise the budget, absent an explicit user decision.

---

## Iteration log

- **iter v1.0 (2026-08-16)** — project home created from the feature_027 completion evaluation. Grounded moves:
  1. Read the evaluation end-to-end (`.sandbox/session_2026-08-15_feature_027_completion.md`, 179 lines) — §2 (8 protections), §3 (12 friction items with COMPOUNDS/FIXED/LOCAL classification), §4 (7 workflow-evolution patterns), §5 (12-item P1/P2/P3 improvement table), §6 (verdict).
  2. Re-verified every code anchor against the current working tree before writing the doc: `gates.py:99-107` (two-hats message), `gates.py:129-131` (diff_snapshots in GREEN hat), `gates.py:156-212` (validate-exit), `gates.py:161-171` (skip_override `scope:"all"`-only), `gates.py:185-197` (full-file coverage scan), `state.py:132` (KNOWN_SUITE_FAILURES), `state.py:157-169` (get_session_records session-scoping — the §3.1 reset root cause), `state.py:181-192` (get_tdd_state), `state.py:232` (diff_snapshots), `state.py:257-272` (run_full_suite), `phase_gate.ts:203-208` (omt_phase writes phase record with tdd_mode), `receipt_guard.ts` + `omt_harness_e2e_last_run.json` (receipt). Confirmed next free feature slot = 028.
  3. Locked the P1/P2/P3 improvement surface as the §Purpose table with verified anchors + per-item Success shapes in §Scope.
  4. Created `CURRENT_STATE.md` iter-0 (session log + resume pointer).
- Gestalt: the v1.0 round was grounded by re-verifying the evaluation's claims against the code rather than trusting the post-mortem's line numbers — the working tree IS the current truth (e.g. the u13 date-drift and kb count-pin items were ALREADY fixed as feature_027 collateral per WORK.md 2026-08-15; v3's P2-5/P2-6 bake the *conventions + lints* so the fix doesn't recur, they do not re-fix the tests).

- **iter v1.1 (2026-08-16)** — pre-build verification: re-read the evaluation evidence + re-verified v1.0's anchors against the current code, found 11 findings (R1–R11), applied inline corrections + this §Plan review section. Grounded moves:
  1. **Read the actual gate code end-to-end** — `gates.py` (212 lines), `state.py` (291 lines), `cli.py` (397 lines), `phase_gate.ts` (396 lines), `ast_checks.py` (203 lines), + the KB-gate substrate (`gate_driver.ts:234`, `session_state.ts:35`, `omt_q.ts:251-285`). Caught two factual errors v1.0 inherited from the evaluation without verifying (R1: the full-suite run is in `cmd_done`/cli.py, NOT `cmd_validate_exit`/gates.py as §3.6 claims; R2: `recent_consults` is the think-consult substrate, not a KB/Read substrate, and `g.kb` is a session-boolean).
  2. **Reframed P1-1 (R3)** — from symptom-level "omt_phase idempotency" to the v3-consistent "feature-scoped TDD state derivation," naming `get_tdd_cycles(feature)` as the existing precedent. This unifies all three P1 items under one thesis.
  3. **Caught a D5 safety regression in P1-2 (R4)** — a naive feature-suite-only split would let real cross-feature regressions ship; added the mandatory `baseline_failures` mechanism (snapshot failing node IDs at Programming entry; `regressions = current − baseline − KNOWN_SUITE_FAILURES`) + a regression golden as a Phase-A exit criterion.
  4. **Sharpened P1-3 (R5)** — `diff_snapshots` does added-methods only (not modified); the per-edit baseline is rolling, not feature-baselined. Specified the feature-baseline snapshot tier as a real substrate extension.
  5. **Committed the P3-8 fold (R7)** — into Phase-A's gates.py round (saves a receipt round); added the HAT_RULES robustness note (R6) so the fix is config-driven.
  6. **Laid out the Phase-A receipt-round + dependency order (R9)** + the hermetic fixtures (R10 hermetic-test-dir for P1-2; R11 two-session ledger for P1-1).
- Gestalt: v1.0 trusted the evaluation's line numbers; v1.1 re-derived every anchor from the code and found that the evaluation's §3.6 had mislocated the suite run and §3.4 had misnamed the consult substrate. The single most leveraged v1.1 finding is R4 — without the `baseline_failures` regression guard, P1-2 would ship a protection regression (real regressions passing through `done`), violating D5. The v3 thesis is unchanged; v1.1 makes the build surface honest about what the existing mechanisms do (added-only diff, rolling snapshot, session-boolean KB gate) so Phase-A designs the right substrate extensions instead of assuming "promote the existing mechanism" is sufficient.

- **iter v1.2 (2026-08-16)** — measured token deep analysis + unified re-scope (user-approved). Grounded moves:
  1. **Real session evidence, not estimates** — read-only aggregate SQL over `opencode.db` (this project's 1009 sessions): 494M input / 8.4M output / 1.07B cache-read tokens, $294.54, 26.2 turns/session avg; tool-result bytes 68.8MB (read 66%, bash 19%, omt_* 4.7%). No session content read — sizes/counts/distributions only.
  2. **Verified lean before proposing cuts** — ceremony tools 257–369B/call, bootstrap tips once-per-session (~690B, nav_gate.ts S6 works as designed), AGENTS.md 2,626/2,816B, schemas 1,094/1,280B, gate messages ~350B. Five prior diet rounds (OPT-A/B/D/H) already cut the static surface; the v1.2 findings are therefore few and specific, not a second diet.
  3. **Three measured leaks isolated** — T1 `omt_q{op:state}` 24–36KB/call (avg 29KB; `decree_health` unconditional, omt_q.ts:494-528 — root-caused in code, not just DB); T2 `omt_nav` uncapped (4.5KB avg × 302); T3 duplicate re-reads (same file up to 29×/session; read = 66% of tool bytes; root cause = compaction eviction, harness leverage indirect). T4 (schema budget at cap) → policy, not a build item.
  4. **User decisions locked** — unified re-scope (D7); slimming-allowed-when-semantics-preserved (D8); T1 shape = summary default + `verbose:true`; schema budget zero-sum (D9); analysis gates the Phase-A build (Phase-A does NOT start until v1.2 is approved as the build surface).
  5. **Caught a stale decision** — D6 (g.kb mirrors `recent_consults`) was already superseded by R2 at v1.1 but never marked; now annotated in place (audit trail preserved).
- Gestalt: the harness's token problem is NOT the standing surface (budgets did their job) — it's three output-shape leaks, and the biggest single one (T1) is in the NEWEST tool (feature_026's omt_q): the interrogative layer shipped without an output budget. Meta-lesson for D9's enforcement: new tools need an output-size budget at design time, not just a schema-description budget at compile time.

---

## References

- **Primary evidence** — `.sandbox/session_2026-08-15_feature_027_completion.md` (the real-session evaluation; §2 protections, §3 friction, §4 patterns, §5 improvement table, §6 verdict).
- **Token evidence (v1.2)** — `~/.local/share/opencode/opencode.db` read-only aggregates over project `b214281b…` (1009 sessions, 2026-04-19→2026-08-16): `session.tokens_*` totals; `part.data` JSON per-tool call counts + output-byte distributions; per-tool averages in §Token evidence (v1.2). Static audit: `.meta/META_HARNESS.omt:241-251` (@budget lines), `.opencode/lib/enforcer/nav_gate.ts:160-181` (bootstrap single-emission), `.opencode/plugins/omt_q.ts:494-528` (T1 root cause).
- **Companion pause doc** — `.sandbox/pause_2026-08-15_l.md` (the iter-l resume anchor the evaluation used; documents the resume-protocol discipline that §2.1 praises).
- **Harness project-home convention** — `.meta/META_HARNESS.omt:184` + `:208` (`.projects/meta/<feature>/{PROJECT.md, CURRENT_STATE.md}` non-gated; PROJECT.md canonical; companion to the phase-gated design doc).
- **Sibling project for convention** — `.projects/meta/meta_harness_2/PROJECT.md` (the v1.5 interrogative-layer project that defined the PROJECT.md + Decisions log + Iteration log + References convention; shipped as feature_026).
- **Gated code anchors (verified 2026-08-16)** — `scripts/omt/tdd/gates.py` (two-hats + validate-exit), `scripts/omt/tdd/state.py` (ledger/session-scoping/known-failures/snapshots), `.opencode/lib/enforcer/phase_gate.ts` (omt_phase/omt_skip/omt_complete), `.opencode/lib/enforcer/receipt_guard.ts` + `.meta/.omt/omt_harness_e2e_last_run.json` (receipt).
- **feature_027 collateral fixes (WORK.md 2026-08-15)** — kb count pins → structural kind-pin; u13 hardcoded date → dynamic now-1h. These ARE the P2-5/P2-6 conventions; v3 bakes them into the gotcha index + lint so the class doesn't recur.